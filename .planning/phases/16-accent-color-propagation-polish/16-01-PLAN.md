---
phase: 16-accent-color-propagation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/admin/settings/settings-ui.tsx
  - src/components/admin/settings/admin-settings-tabs.tsx
  - src/components/admin/settings/profile-tab.tsx
  - src/components/admin/dashboard/welcome-banner.tsx
  - src/components/admin/dashboard/activity-chart.tsx
  - src/components/admin/dashboard/functionari-performance.tsx
  - src/components/admin/dashboard/system-health-section.tsx
  - src/components/admin/dashboard/admin-alerts-panel.tsx
  - src/components/admin/dashboard/MetricCard.tsx
  - src/components/admin/dashboard/StatisticsCards.tsx
  - src/components/admin/dashboard/MetricDetailsModal.tsx
  - src/components/admin/dashboard/chart-utils.ts
  - src/components/notifications/NotificationDropdown.tsx
  - src/components/shell/TopBarActions.tsx
  - src/components/shell/SidebarNavItem.tsx
  - src/app/api/admin/service-breakdown/route.ts
  - src/lib/admin-dashboard-queries.ts
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-3

must_haves:
  truths:
    - "Changing accent color in Appearance tab updates ALL gradient buttons, toggles, chart highlights, avatar backgrounds, tab indicators, and welcome banner"
    - "No hardcoded pink/violet hex values (#ec4899, #8b5cf6, #a855f7, #6366f1, #f43f5e) remain in admin, shell, or notification components"
    - "Gradient buttons use dynamic algorithm: accent-500 + hue-shifted +30deg (not fixed hex pairs)"
    - "Sidebar active item shows accent-colored left bar, accent-tinted background, accent-colored icon + text in both light and dark mode"
    - "Admin settings visual treatment matches Figma reference: inline input icons, gradient save buttons, card contrast, tab indicator all use accent tokens"
  artifacts:
    - path: "src/app/globals.css"
      provides: "--accent-gradient and --accent-gradient-subtle CSS custom properties"
      contains: "accent-gradient"
    - path: "src/components/shell/SidebarNavItem.tsx"
      provides: "Accent-colored active state with left bar indicator"
      contains: "accent"
  key_links:
    - from: "src/app/globals.css"
      to: "all admin components"
      via: "var(--accent-gradient) CSS custom property"
      pattern: "var\\(--accent-gradient\\)"
    - from: "src/store/accent-color-store.ts"
      to: "src/app/globals.css"
      via: "--accent-hue CSS variable set on documentElement"
      pattern: "--accent-hue"
---

<objective>
Define dynamic accent gradient CSS custom properties and systematically replace all hardcoded pink/violet hex values across 16+ files so every UI element responds to accent color selection. Also verify that admin settings visual treatment (inline input icons, gradient save buttons, card contrast, tab indicator) uses dynamic accent tokens matching Figma reference (SC-3).

Purpose: SC-1 (accent color updates ALL elements), SC-2 (dynamic gradient algorithm), and SC-3 (Figma visual match uses accent tokens, not hardcoded colors).
Output: `--accent-gradient` and `--accent-gradient-subtle` in globals.css, all admin/shell/notification components using dynamic tokens.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-accent-color-propagation-polish/16-CONTEXT.md
@.planning/phases/16-accent-color-propagation-polish/16-RESEARCH.md
@src/app/globals.css
@src/store/accent-color-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accent gradient CSS custom properties and fix dark mode sidebar tokens</name>
  <files>src/app/globals.css</files>
  <action>
In globals.css `:root` block, after the existing `--accent-950` line, add two new CSS custom properties:

```css
--accent-gradient: linear-gradient(
  135deg,
  var(--accent-500),
  oklch(0.573 0.13 calc(var(--accent-hue) + 30))
);
--accent-gradient-subtle: linear-gradient(
  135deg,
  oklch(0.648 0.147 var(--accent-hue) / 0.6),
  oklch(0.573 0.13 calc(var(--accent-hue) + 30) / 0.4)
);
```

In the `.dark` block, fix the sidebar tokens that currently use static oklch values instead of accent references:
- Change `--sidebar-primary` to `var(--accent-500)` (currently static white/light oklch value)
- Ensure `--sidebar-accent` also references accent tokens if it has a static value

Also add an accent shadow utility for box-shadows:
```css
--accent-shadow: oklch(0.648 0.147 var(--accent-hue) / 0.2);
```

Do NOT modify the existing accent-50 through accent-950 scale -- those are correct.
Do NOT modify the `@theme inline` block unless needed for Tailwind utility class registration of the new properties.
  </action>
  <verify>
    <automated>grep -n "accent-gradient" src/app/globals.css | head -10 && grep -n "sidebar-primary" src/app/globals.css</automated>
  </verify>
  <done>globals.css has --accent-gradient, --accent-gradient-subtle, --accent-shadow defined in :root. Dark mode .dark block has sidebar-primary referencing var(--accent-500) instead of static oklch.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all hardcoded pink/violet hex values with dynamic accent tokens</name>
  <files>
    src/components/admin/settings/settings-ui.tsx,
    src/components/admin/settings/admin-settings-tabs.tsx,
    src/components/admin/settings/profile-tab.tsx,
    src/components/admin/dashboard/welcome-banner.tsx,
    src/components/admin/dashboard/activity-chart.tsx,
    src/components/admin/dashboard/functionari-performance.tsx,
    src/components/admin/dashboard/system-health-section.tsx,
    src/components/admin/dashboard/admin-alerts-panel.tsx,
    src/components/admin/dashboard/MetricCard.tsx,
    src/components/admin/dashboard/StatisticsCards.tsx,
    src/components/admin/dashboard/MetricDetailsModal.tsx,
    src/components/admin/dashboard/chart-utils.ts,
    src/components/notifications/NotificationDropdown.tsx,
    src/components/shell/TopBarActions.tsx,
    src/components/shell/SidebarNavItem.tsx,
    src/app/api/admin/service-breakdown/route.ts,
    src/lib/admin-dashboard-queries.ts
  </files>
  <action>
Systematically replace hardcoded hex values following the research replacement map. Read each file first, then apply replacements:

**Buttons/Toggles (full-strength gradient):**
- `settings-ui.tsx`: GradientSaveButton and AnimatedToggle -- replace `linear-gradient(135deg, #ec4899, #8b5cf6)` with `var(--accent-gradient)`
- `profile-tab.tsx`: avatar gradient `#8b5cf6, #6366f1` -> `var(--accent-gradient)`

**Tab indicators:**
- `admin-settings-tabs.tsx`: replace `rgba(236,72,153,...)` with `bg-accent-500/[0.08]` and `border-accent-500/[0.12]` Tailwind classes. Replace `text-pink-400` with `text-accent-500`.

**Welcome banner (subtle gradient):**
- `welcome-banner.tsx`: replace 3-stop gradient `#ec4899, #a855f7, #6366f1` with `var(--accent-gradient-subtle)`. Replace boxShadow hardcoded rgba with `0 8px 32px var(--accent-shadow)`.

**Charts:**
- `activity-chart.tsx`: replace `#ec4899, #f43f5e` gradient and `text-pink-400` with accent tokens
- `chart-utils.ts`: replace `#8b5cf6` and `#ec4899` in color palette array first slot with `var(--accent-500)` (for CSS usage) or keep as fallback noting accent will override via component props
- `MetricCard.tsx`: replace `#a855f7` sparkline color with accent reference
- `StatisticsCards.tsx`: replace `#8b5cf6` with accent reference
- `MetricDetailsModal.tsx`: replace `#8b5cf6, #ec4899` in color array with accent references

**Dashboard panels (preserve semantic colors):**
- `system-health-section.tsx`: ONLY replace `#8b5cf6` (API Response -- decorative). KEEP `#10b981` (green=healthy), `#3b82f6` (blue=info), `#f59e0b` (amber=warning).
- `admin-alerts-panel.tsx`: ONLY replace `#8b5cf6` (system severity -- decorative). KEEP `#ef4444` (red=urgent), `#f59e0b` (yellow=warning), `#3b82f6` (blue=info).
- `admin-dashboard-queries.ts`: KEEP `#8b5cf6` for `in_verificare` status (semi-semantic status indicator, not decorative).

**Notifications:**
- `NotificationDropdown.tsx`: replace `bg-[#be3144]` badge (x2 occurrences) with `bg-accent-500`
- `TopBarActions.tsx`: replace badge color with `bg-accent-500` per user decision (notification badges use accent)

**Sidebar:**
- `SidebarNavItem.tsx`: update active state classes to use accent tokens:
  - Active item: `bg-accent-500/10 text-accent-500` for background and text
  - Active left bar indicator: `before:bg-accent-500` (pseudo-element)
  - Icon tinting: add `text-accent-500` to icon when active
  - Hover on inactive: `hover:bg-accent-500/5` for faint accent tint
  - Ensure collapsed sidebar (icon-only) also tints active icon to accent

**API route:**
- `service-breakdown/route.ts`: replace primary chart palette slot `#8b5cf6` with a comment noting it should use accent, OR replace with a neutral that the client-side component will override.

For Recharts components that accept `fill` or `stroke` props as strings: use inline style `var(--accent-500)` where the component accepts CSS strings, or use the existing colorVariantMap pattern with accent token classes where Tailwind classes are used.

IMPORTANT: Do NOT use `oklch(from ... l c h / alpha)` relative color syntax (browser support uncertain). Use Tailwind opacity modifier syntax instead: `bg-accent-500/10`, `border-accent-500/12`, etc.

**SC-3 verification:** After all replacements, verify that admin settings components (settings-ui.tsx, admin-settings-tabs.tsx, profile-tab.tsx) use accent tokens for: inline input icons (InputWithIcon icon color), gradient save buttons (GradientSaveButton), card contrast (settings card backgrounds), and tab indicator (active tab highlight). These are the Figma visual match elements. If any of these still use hardcoded colors, replace them with accent tokens.
  </action>
  <verify>
    <automated>echo "=== Settings components ===" && grep -cn "#ec4899\|#8b5cf6\|#a855f7\|#6366f1\|#f43f5e\|text-pink-400" src/components/admin/settings/settings-ui.tsx src/components/admin/settings/admin-settings-tabs.tsx src/components/admin/settings/profile-tab.tsx 2>/dev/null && echo "=== Dashboard components ===" && grep -cn "#ec4899\|#8b5cf6\|#a855f7\|#6366f1\|#f43f5e" src/components/admin/dashboard/welcome-banner.tsx src/components/admin/dashboard/activity-chart.tsx src/components/admin/dashboard/MetricCard.tsx src/components/admin/dashboard/StatisticsCards.tsx src/components/admin/dashboard/MetricDetailsModal.tsx src/components/admin/dashboard/functionari-performance.tsx 2>/dev/null && echo "=== Shell and notifications ===" && grep -cn "#ec4899\|#8b5cf6\|bg-\[#be3144\]\|text-pink-400" src/components/shell/SidebarNavItem.tsx src/components/shell/TopBarActions.tsx src/components/notifications/NotificationDropdown.tsx 2>/dev/null && echo "=== Semantic preserves (should have counts) ===" && grep -cn "#10b981\|#ef4444\|#f59e0b\|#3b82f6" src/components/admin/dashboard/system-health-section.tsx src/components/admin/dashboard/admin-alerts-panel.tsx 2>/dev/null</automated>
  </verify>
  <done>Zero hardcoded pink/violet hex values in settings (0), dashboard decorative (0), shell/notification (0) components. Semantic colors in system-health and alerts panels preserved (non-zero counts for green/red/yellow/blue). Admin settings visual elements (input icons, gradient buttons, card contrast, tab indicator) all use dynamic accent tokens matching Figma reference. grep per-group counts all return 0 for hardcoded hex, non-zero for semantic colors.</done>
</task>

</tasks>

<verification>
1. Settings group: `grep -cn "#ec4899\|#8b5cf6\|#a855f7\|text-pink-400" src/components/admin/settings/settings-ui.tsx src/components/admin/settings/admin-settings-tabs.tsx src/components/admin/settings/profile-tab.tsx` returns 0 for each file
2. Dashboard group: `grep -cn "#ec4899\|#8b5cf6\|#a855f7\|#6366f1\|#f43f5e" src/components/admin/dashboard/welcome-banner.tsx src/components/admin/dashboard/activity-chart.tsx src/components/admin/dashboard/MetricCard.tsx src/components/admin/dashboard/StatisticsCards.tsx src/components/admin/dashboard/MetricDetailsModal.tsx` returns 0 for each file
3. Shell/notification group: `grep -cn "#ec4899\|#8b5cf6\|bg-\[#be3144\]\|text-pink-400" src/components/shell/SidebarNavItem.tsx src/components/shell/TopBarActions.tsx src/components/notifications/NotificationDropdown.tsx` returns 0 for each file
4. Semantic colors preserved: `grep -c "#10b981\|#ef4444\|#f59e0b\|#3b82f6" src/components/admin/dashboard/system-health-section.tsx` returns non-zero
5. `grep "accent-gradient" src/app/globals.css` shows both --accent-gradient and --accent-gradient-subtle
6. `pnpm build` completes without errors
7. `pnpm type-check` passes
</verification>

<success_criteria>
- --accent-gradient and --accent-gradient-subtle defined as CSS custom properties in globals.css
- Dark mode sidebar tokens reference var(--accent-500)
- All 16 files audited and hardcoded decorative hex values replaced
- Semantic colors (green=healthy, red=urgent, yellow=warning, blue=info) preserved unchanged
- Sidebar active item uses accent-colored left bar, background, icon, and text
- Admin settings Figma visual match elements (inline input icons, gradient save buttons, card contrast, tab indicator) all use dynamic accent tokens (SC-3)
- Build and type-check pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-accent-color-propagation-polish/16-01-SUMMARY.md`
</output>
