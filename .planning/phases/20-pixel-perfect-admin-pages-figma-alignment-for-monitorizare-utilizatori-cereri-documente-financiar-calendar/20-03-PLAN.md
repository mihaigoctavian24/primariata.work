---
phase: 20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar
plan: "03"
type: execute
wave: 4
depends_on: ["20-02"]
files_modified:
  - src/app/app/[judet]/[localitate]/admin/financiar/page.tsx
  - src/components/admin/financiar/financiar-content.tsx
  - src/components/admin/financiar/financiar-skeleton.tsx
  - src/components/admin/financiar/kpi-cards.tsx
  - src/components/admin/financiar/revenue-charts.tsx
  - src/components/admin/financiar/transaction-list.tsx
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04
  - FIN-05
must_haves:
  truths:
    - "Admin sees 6 KPI cards with real data from plati table: total colectat, target progress, nr tranzacții, rată succes, rată eșec, valoare medie"
    - "Monthly revenue AreaChart renders real aggregated data from plati table (colectat vs mock target)"
    - "Daily volume BarChart renders real aggregated data grouped by day of week"
    - "Filterable transaction list shows real plati rows with status badges, expandable row with retry/details"
    - "pnpm build succeeds with zero TypeScript errors"
  artifacts:
    - path: "src/components/admin/financiar/financiar-content.tsx"
      provides: "Client Component coordinator for all financiar sections"
      exports: ["FinanciarContent"]
    - path: "src/components/admin/financiar/kpi-cards.tsx"
      provides: "6 KPI stat cards + target progress bar"
      exports: ["KpiCards"]
    - path: "src/components/admin/financiar/revenue-charts.tsx"
      provides: "Monthly AreaChart + daily BarChart + payment methods list + category grid"
      exports: ["RevenueCharts"]
    - path: "src/components/admin/financiar/transaction-list.tsx"
      provides: "Filterable transaction list with pagination and expandable rows"
      exports: ["TransactionList"]
    - path: "src/components/admin/financiar/financiar-skeleton.tsx"
      provides: "Loading skeleton"
      exports: ["FinanciarSkeleton"]
  key_links:
    - from: "src/app/app/[judet]/[localitate]/admin/financiar/page.tsx"
      to: "src/lib/financiar-utils.ts"
      via: "aggregateByMonth, groupByDayOfWeek, groupByMetoda imports — server-side aggregation"
      pattern: "financiar-utils"
    - from: "financiar-content.tsx"
      to: "revenue-charts.tsx"
      via: "passes monthlyData, dailyData, metodaData as props"
      pattern: "RevenueCharts"
---

<objective>
Wave 3 — Full rewrite of the Financiar admin page to pixel-perfect Figma match with real plati DB data.

Purpose: Replace Phase 19 Financiar components (delete and rewrite) with Figma-accurate financials: 6 KPI cards, monthly revenue chart, daily volume chart, payment methods breakdown, category grid, and a filterable transaction list. All data from the real plati table via existing financiar-utils.ts aggregations.

Output: 5 files in src/components/admin/financiar/ + updated page.tsx. Real plati data from DB.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-CONTEXT.md
@.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-RESEARCH.md

<interfaces>
<!-- Figma source: Revamp Primarie Admin/src/app/components/pages/PlatiPage.tsx (613 lines) -->
<!-- Key Figma visual elements (adapt to Next.js):
  - 6 KPI cards in a responsive grid: Colectat, Target, Tranzacții, Rată succes, Rată eșec, Val. medie
  - Progress bar under "Colectat" showing % of monthly target (mock target: 210000 RON)
  - 4 tx status mini-cards: Succes/Pending/Eșuat/Rambursate — clicking filters the transaction list
  - Revenue AreaChart: 6m/1y toggle, "Colectat" + "Eșuat" areas, gradient fills
  - Daily volume BarChart: Lun-Dum, tranzacții count per day
  - Payment methods: bar list with percentage + count (Card Online, Transfer, ghișeu.ro, etc.)
  - Category grid: 6 categories with animated progress bars (colectat vs target)
  - Transaction list: search + status filter, expandable row with errorCode display + retry button
  - Colors in Recharts SVG stroke/fill use hex (CSS var exception)
-->

<!-- Existing financiar-utils.ts exports (reuse server-side): -->
import { aggregateByMonth, groupByDayOfWeek, groupByMetoda } from "@/lib/financiar-utils";
// aggregateByMonth: PlatiRow[] → Array<{ month: string; colectat: number; esuat: number }>
// groupByDayOfWeek: PlatiRow[] → Array<{ day: string; tranzactii: number; valoare: number }>
// groupByMetoda: PlatiRow[] → Record<string, { count: number; total: number }>

<!-- FinanciarContent props (from existing page.tsx — keep as-is): -->
interface FinanciarContentProps {
  plati: Array<{
    id: string; suma: number; status: string;
    metoda_plata: string | null; created_at: string; cerere_id: string | null;
  }>;
  monthlyData: Array<{ month: string; colectat: number; esuat: number }>;
  dailyData: Array<{ day: string; tranzactii: number; valoare: number }>;
  metodaData: Record<string, { count: number; total: number }>;
  tipuriCereri: Array<{ id: string; nume: string }>;
  totalRevenue: number;
}

<!-- Category breakdown: graceful mock fallback (Phase 19 decision) -->
// categoryBreakdown uses graceful mock if no category data in DB
// Phase 19 STATE.md: "Category progress bars use graceful mock fallback (Stare Civilă/Urbanism/Social)"
const MOCK_CATEGORIES = [
  { name: "Taxe Locale", colectat: 0, target: 50000, color: "#3b82f6" },
  { name: "Autorizații", colectat: 0, target: 20000, color: "#8b5cf6" },
  { name: "Amenzi", colectat: 0, target: 15000, color: "#ef4444" },
  { name: "Certificări", colectat: 0, target: 8000, color: "#10b981" },
  { name: "Impozite Proprietăți", colectat: 0, target: 12000, color: "#f59e0b" },
  { name: "Altele", colectat: 0, target: 5000, color: "#6b7280" },
];

<!-- Transaction reference/gateway: plati table may not have these columns -->
<!-- Per research open question 3: show "—" if reference/gateway not in DB schema -->
<!-- Do NOT add columns that don't exist. Use plati.id as display reference. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Phase 19 financiar files + create skeleton + kpi-cards + revenue-charts</name>
  <files>
    src/components/admin/financiar/financiar-skeleton.tsx
    src/components/admin/financiar/kpi-cards.tsx
    src/components/admin/financiar/revenue-charts.tsx
  </files>
  <action>
    **Step 0 — Delete Phase 19 files:**
    ```bash
    rm -rf src/components/admin/financiar/
    mkdir -p src/components/admin/financiar/
    ```

    **Step 1 — Create financiar-skeleton.tsx:**

    Animated pulse skeleton: 6 card placeholders in a grid + 2 chart area placeholders + table rows. All `bg-white/[0.06] animate-pulse rounded-2xl`.

    **Step 2 — Create kpi-cards.tsx:**

    Props: `{ plati: FinanciarContentProps["plati"]; totalRevenue: number }`

    Compute client-side (from plati array):
    - totalTx: plati.length
    - successTx: filter status "success"
    - failedTx: filter status "failed"
    - pendingTx: filter status "pending"
    - refundedTx: filter status "refunded"
    - successRate: (successTx / totalTx * 100) rounded to 1dp — show 0 if totalTx === 0
    - failRate: same
    - avgTxValue: totalRevenue / successTx — show 0 if successTx === 0
    - monthTarget: 210000 (mock constant — stays mock per CONTEXT.md deferred list)
    - targetProgress: Math.round((totalRevenue / monthTarget) * 100)

    6 KPI cards in a `grid grid-cols-2 md:grid-cols-3 xl:grid-cols-6 gap-4` layout:
    Each card: `bg-white/[0.025] border border-white/[0.05] rounded-2xl p-4` with:
    - Icon (colored), label text, value (use AnimatedCounter from shared/ for integer values), trend arrow
    - Colectat card has animated progress bar under the value (targetProgress %)

    4 tx status mini-cards below KPI grid:
    - Succes/Pending/Eșuat/Rambursate — each clickable, triggers `onFilterChange(status)` callback
    - Props: `{ txFilter: string; onFilterChange: (f: string) => void }`

    Framer Motion: card entry animations with stagger.

    **Step 3 — Create revenue-charts.tsx:**

    Props: `{ monthlyData: ...; dailyData: ...; metodaData: ... }`

    Layout: `grid grid-cols-1 xl:grid-cols-2 gap-5`:

    **Left panel — Revenue AreaChart:**
    - 6m/1y period toggle (useState — filtering monthlyData by slice)
    - ResponsiveContainer height 200px
    - 2 Areas: "colectat" (#10b981) + "esuat" (#ef4444)
    - Gradient defs: `colectatGrad20` and `esuatGrad20` (unique gradient IDs to avoid SVG conflict)
    - CustomTooltip: `bg-popover border-border` via CSS tokens (not hardcoded dark)
    - XAxis/YAxis: `stroke="#6b7280"`, `fontSize={11}`

    **Right panel — Daily Volume BarChart:**
    - ResponsiveContainer height 200px
    - Bar fill "#3b82f6" (hex — SVG attribute)
    - Shows `day` on X axis, `tranzactii` on Y axis

    **Below left — Payment Methods list:**
    - Map metodaData to sorted list (by count desc)
    - Each row: icon, name, count, percentage bar animated with motion.div
    - Bar background: `bg-white/[0.06]`, fill: Tailwind color class per method

    **Below right — Category breakdown grid:**
    - 6 items in `grid grid-cols-2 gap-3`
    - Each: name, colectat/target amounts, animated progress bar (motion.div initial width 0 → animate to %)
    - Use MOCK_CATEGORIES (graceful fallback) — actual cereri category data not in plati table schema
    - NOTE: This section is intentionally mock data. Add a comment: `// Category data: graceful mock — plati table has no category column`
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -E "error TS|Error:" | head -10 || echo "Build clean"</automated>
  </verify>
  <done>
    financiar-skeleton, kpi-cards, revenue-charts created. pnpm build passes. Recharts SVG strokes use hex. CustomTooltip uses CSS tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create transaction-list + financiar-content coordinator</name>
  <files>
    src/components/admin/financiar/transaction-list.tsx
    src/components/admin/financiar/financiar-content.tsx
  </files>
  <action>
    **Step 1 — Create transaction-list.tsx:**

    Props: `{ plati: FinanciarContentProps["plati"]; txFilter: string; onFilterChange: (f: string) => void }`

    Features:
    - Search bar (filters by plati.id since reference/gateway columns may not exist in DB)
    - Status filter controlled from parent (txFilter prop) — synced with mini-cards in kpi-cards
    - Table columns: ID (truncated), Suma, Metodă, Status, Data, Acțiuni
    - 6 per page, pagination
    - Expandable row: shows cerere_id if set, metoda_plata, error hint if status "failed" ("Tranzacție eșuată — contactați furnizorul")
    - Retry button for failed tx: `toast.info("Reîncercare în curs...")` (mock — no real retry API for academic milestone)
    - Status badges: use Tailwind classes (emerald for success, amber for pending, red for failed, violet for refunded)
    - Empty state: "Nicio tranzacție" with Receipt icon

    Framer Motion: `AnimatePresence mode="popLayout"` on rows with `layout`.

    **Step 2 — Create financiar-content.tsx (coordinator):**

    `"use client"`. Receives all props from page.tsx:
    ```typescript
    interface FinanciarContentProps {
      plati: Array<{ id: string; suma: number; status: string; metoda_plata: string | null; created_at: string; cerere_id: string | null }>;
      monthlyData: Array<{ month: string; colectat: number; esuat: number }>;
      dailyData: Array<{ day: string; tranzactii: number; valoare: number }>;
      metodaData: Record<string, { count: number; total: number }>;
      tipuriCereri: Array<{ id: string; nume: string }>;
      totalRevenue: number;
    }
    ```

    State: `txFilter: string` (synced between KpiCards mini-buttons and TransactionList).

    Layout (vertical stacking):
    1. Page header: "Financiar" title with CreditCard icon + subtitle "{totalRevenue} RON colectat"
    2. KpiCards (with onFilterChange handler)
    3. RevenueCharts (monthly + daily + methods + categories)
    4. Section header "Tranzacții Recente"
    5. TransactionList (with txFilter + onFilterChange)

    Framer Motion: `motion.div` page entry with `initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}`.

    All hardcoded dark backgrounds → CSS tokens:
    - Card backgrounds: `bg-white/[0.025] border border-white/[0.05]`
    - Text muted: `text-gray-400` (Tailwind, not inline style)
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -E "error TS|Error:" | head -10 || echo "Build clean"</automated>
  </verify>
  <done>
    transaction-list and financiar-content created. FinanciarContent wires all sections. pnpm build green. txFilter state shared between KpiCards and TransactionList.
  </done>
</task>

</tasks>

<verification>
After Wave 3:
- pnpm build passes with zero TypeScript errors
- Financiar page loads with real plati data
- KPI cards show correct computed values from DB data
- Revenue AreaChart renders colectat vs esuat areas with Recharts hex strokes
- Transaction list filterable by status
- Category section graceful mock with comment
- No hardcoded #1a1a2e dark-only backgrounds in tooltip or cards
</verification>

<success_criteria>
- Admin sees real financial data from plati table
- All chart sections render in both light and dark themes
- Transaction list pagination works
- KPI mini-card status filter syncs with transaction list
- animatedCounter values count up on scroll into view
</success_criteria>

<output>
After completion, create `.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-03-SUMMARY.md`
</output>
