---
phase: 20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/admin/shared/animated-counter.tsx
  - src/components/admin/shared/progress-ring.tsx
  - src/components/admin/shared/donut-chart.tsx
  - src/components/admin/shared/stats-card.tsx
  - src/components/admin/shared/live-activity-feed.tsx
  - src/components/admin/shared/gauge-svg.tsx
  - src/components/admin/shared/role-color-badge.tsx
autonomous: true
requirements:
  - UTL-01
  - MON-01
must_haves:
  truths:
    - "Role color tokens (--role-cetatan, --role-functionar, --role-primar, --role-admin) are defined in globals.css and visually distinct in both light and dark modes"
    - "src/components/admin/shared/ directory exists with 7 migrated/new shared components, all importing cleanly"
    - "pnpm build succeeds with zero TypeScript errors after this wave"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Role color CSS custom properties in :root and .dark blocks"
      contains: "--role-cetatan"
    - path: "src/components/admin/shared/animated-counter.tsx"
      provides: "Migrated AnimatedCounter from admin/ with LucideIcon-compatible types"
      exports: ["AnimatedCounter", "AnimatedCounterProps"]
    - path: "src/components/admin/shared/gauge-svg.tsx"
      provides: "NEW circular SVG gauge for CPU/RAM/Disk in Monitorizare"
      exports: ["GaugeSVG", "GaugeSVGProps"]
    - path: "src/components/admin/shared/role-color-badge.tsx"
      provides: "NEW badge using --role-* token classes"
      exports: ["RoleColorBadge"]
    - path: "src/components/admin/shared/donut-chart.tsx"
      provides: "Migrated DonutChart from admin/"
      exports: ["DonutChart"]
    - path: "src/components/admin/shared/stats-card.tsx"
      provides: "Migrated StatsCard from admin/"
      exports: ["StatsCard"]
    - path: "src/components/admin/shared/live-activity-feed.tsx"
      provides: "Migrated LiveActivityFeed from admin/"
      exports: ["LiveActivityFeed"]
    - path: "src/components/admin/shared/progress-ring.tsx"
      provides: "Migrated ProgressRing from admin/"
      exports: ["ProgressRing"]
  key_links:
    - from: "src/components/admin/shared/"
      to: "src/components/admin/*/index.ts"
      via: "named re-exports"
      pattern: "from.*@/components/admin/shared"
    - from: "src/app/globals.css"
      to: "all admin page components"
      via: "CSS custom properties cascade"
      pattern: "--role-cetatan|--role-functionar|--role-primar|--role-admin"
---

<objective>
Wave 0 — Foundation prep before any page rewrite begins.

Purpose: All subsequent waves (1-6) depend on role color tokens in globals.css and shared components in src/components/admin/shared/. Without this wave, downstream tasks have import errors and missing CSS tokens.

Output: globals.css with 4 role color tokens, shared/ directory with 7 components ready for import.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-CONTEXT.md
@.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-RESEARCH.md

<interfaces>
<!-- Phase 19 animated-counter.tsx — migrate to shared/ as-is (high quality, zero rewrites needed) -->
<!-- File: src/components/admin/animated-counter.tsx -->
export interface AnimatedCounterProps {
  target: number;
  duration?: number;
  className?: string;
  style?: React.CSSProperties;
  formatFn?: (n: number) => string;
}
function AnimatedCounter({ target, duration, className, style, formatFn }: AnimatedCounterProps): React.ReactElement
export { AnimatedCounter };
export type { AnimatedCounterProps };

<!-- GaugeSVG — NEW component adapted from Figma MonitorizarePage.tsx CircularGauge -->
<!-- SVG stroke/color must be hex (SVG attribute — CSS vars don't resolve in SVG stroke) -->
export interface GaugeSVGProps {
  value: number;   // current numeric value
  max: number;     // scale maximum (100 for %, or custom for memory MB, etc.)
  color: string;   // hex — required for SVG stroke attribute
  label: string;   // displayed below gauge (e.g., "CPU")
  unit: string;    // displayed after value (e.g., "%", "ms")
  size?: number;   // outer diameter in px, default 80
}
function GaugeSVG(props: GaugeSVGProps): React.ReactElement

<!-- RoleColorBadge — NEW, uses --role-* token Tailwind classes -->
type RoleKey = "cetatean" | "functionar" | "primar" | "admin";
const roleClasses: Record<RoleKey, string> = {
  cetatean:   "bg-blue-500/15 text-blue-500",
  functionar: "bg-emerald-500/15 text-emerald-500",
  primar:     "bg-amber-500/15 text-amber-500",
  admin:      "bg-violet-500/15 text-violet-500",
};
function RoleColorBadge({ role, className }: { role: RoleKey; className?: string }): React.ReactElement
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role color tokens to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    Open src/app/globals.css and add 4 role color custom properties inside the existing @layer base block.

    Add these lines to the :root block (after the existing --destructive/--border tokens, before any closing }):

    ```css
    /* ─── Role Color Tokens ─── */
    --role-cetatan: var(--color-blue-500);
    --role-functionar: var(--color-emerald-500);
    --role-primar: var(--color-amber-500);
    --role-admin: var(--color-violet-500);
    ```

    Also add the same 4 lines inside the .dark block (same values — Tailwind 4 color-* tokens are theme-aware through oklch).

    Tailwind 4 automatically exposes --color-blue-500, --color-emerald-500 etc. as CSS variables in @layer base. No additional setup needed.

    The values map exactly to the Figma hex colors:
    - blue-500 = #3b82f6 (cetățean)
    - emerald-500 = #10b981 (funcționar)
    - amber-500 = #f59e0b (primar)
    - violet-500 = #8b5cf6 (admin)
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>globals.css contains all 4 --role-* tokens in both :root and .dark. pnpm build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared/ directory with migrated + new components</name>
  <files>
    src/components/admin/shared/animated-counter.tsx
    src/components/admin/shared/progress-ring.tsx
    src/components/admin/shared/donut-chart.tsx
    src/components/admin/shared/stats-card.tsx
    src/components/admin/shared/live-activity-feed.tsx
    src/components/admin/shared/gauge-svg.tsx
    src/components/admin/shared/role-color-badge.tsx
  </files>
  <action>
    Create the directory src/components/admin/shared/ and place 7 files inside it.

    **Files 1-5: Migrate existing Phase 19 shared components**

    Copy these files from src/components/admin/ to src/components/admin/shared/ with one change each: update any internal cross-import paths if needed, and verify no `any` types remain (replace `any` with `LucideIcon` for icon props, `unknown` for truly unknown data shapes).

    Files to migrate:
    - animated-counter.tsx — copy as-is (already clean, no any types)
    - progress-ring.tsx — copy, verify icon type if any
    - donut-chart.tsx — copy, replace any icon-typed props with `LucideIcon` from lucide-react
    - stats-card.tsx — copy, replace any icon-typed props with `LucideIcon` from lucide-react
    - live-activity-feed.tsx — copy, replace any icon-typed props with `LucideIcon` from lucide-react

    Do NOT delete the originals in src/components/admin/ yet — they may still be imported by existing dashboard components. Downstream waves will handle cleanup.

    **File 6: gauge-svg.tsx (NEW)**

    ```typescript
    "use client";

    import { motion } from "motion/react";

    interface GaugeSVGProps {
      value: number;
      max: number;
      color: string; // hex — SVG stroke attribute cannot use CSS vars
      label: string;
      unit: string;
      size?: number;
    }

    function GaugeSVG({ value, max, color, label, unit, size = 80 }: GaugeSVGProps): React.ReactElement {
      const clampedValue = Math.min(Math.max(value, 0), max);
      const pct = (clampedValue / max) * 100;
      const strokeWidth = 7;
      const r = (size - strokeWidth) / 2;
      const circ = 2 * Math.PI * r;
      const offset = circ - (pct / 100) * circ;
      const center = size / 2;

      return (
        <div className="flex flex-col items-center gap-1">
          <div className="relative" style={{ width: size, height: size }}>
            <svg width={size} height={size} style={{ transform: "rotate(-90deg)" }}>
              {/* Track */}
              <circle
                cx={center}
                cy={center}
                r={r}
                fill="none"
                stroke="rgba(255,255,255,0.06)"
                strokeWidth={strokeWidth}
              />
              {/* Progress arc */}
              <motion.circle
                cx={center}
                cy={center}
                r={r}
                fill="none"
                stroke={color}
                strokeWidth={strokeWidth}
                strokeLinecap="round"
                strokeDasharray={circ}
                initial={{ strokeDashoffset: circ }}
                animate={{ strokeDashoffset: offset }}
                transition={{ duration: 1.2, ease: "easeOut" }}
                style={{
                  filter: `drop-shadow(0 0 6px ${color}60)`,
                }}
              />
            </svg>
            {/* Center label */}
            <div
              className="absolute inset-0 flex flex-col items-center justify-center"
              style={{ transform: "rotate(0deg)" }}
            >
              <span className="text-white font-bold" style={{ fontSize: size * 0.22 }}>
                {Math.round(clampedValue)}{unit}
              </span>
            </div>
          </div>
          <span className="text-gray-400 text-xs">{label}</span>
        </div>
      );
    }

    export { GaugeSVG };
    export type { GaugeSVGProps };
    ```

    **File 7: role-color-badge.tsx (NEW)**

    ```typescript
    "use client";

    type RoleKey = "cetatean" | "functionar" | "primar" | "admin";

    const roleClasses: Record<RoleKey, string> = {
      cetatean:   "bg-blue-500/15 text-blue-500",
      functionar: "bg-emerald-500/15 text-emerald-500",
      primar:     "bg-amber-500/15 text-amber-500",
      admin:      "bg-violet-500/15 text-violet-500",
    };

    const roleLabels: Record<RoleKey, string> = {
      cetatean:   "Cetățean",
      functionar: "Funcționar",
      primar:     "Primar",
      admin:      "Admin",
    };

    interface RoleColorBadgeProps {
      role: RoleKey;
      className?: string;
    }

    function RoleColorBadge({ role, className = "" }: RoleColorBadgeProps): React.ReactElement {
      const classes = roleClasses[role] ?? "bg-gray-500/15 text-gray-400";
      const label = roleLabels[role] ?? role;
      return (
        <span
          className={`inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium ${classes} ${className}`}
        >
          {label}
        </span>
      );
    }

    export { RoleColorBadge };
    export type { RoleColorBadgeProps, RoleKey };
    ```
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -E "error|Error|warn" | head -20 || echo "Build clean"</automated>
  </verify>
  <done>
    src/components/admin/shared/ contains 7 files. All named exports. No `any` types. pnpm build succeeds.
    GaugeSVG renders a circular SVG ring with animated stroke. RoleColorBadge uses Tailwind opacity classes.
  </done>
</task>

</tasks>

<verification>
After Wave 0:
- pnpm build passes with zero TypeScript errors
- src/components/admin/shared/ has 7 files
- globals.css has --role-cetatan, --role-functionar, --role-primar, --role-admin tokens in :root and .dark
- No existing admin components were broken (originals not deleted)
</verification>

<success_criteria>
- All 7 shared components exportable via named exports
- No any types in new/migrated files
- Role color tokens defined in CSS
- pnpm build green
</success_criteria>

<output>
After completion, create `.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-00-SUMMARY.md`
</output>
