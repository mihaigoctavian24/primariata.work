---
phase: 20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar
plan: "01"
type: execute
wave: 2
depends_on: ["20-00"]
files_modified:
  - src/app/app/[judet]/[localitate]/admin/users/page.tsx
  - src/components/admin/utilizatori/utilizatori-content.tsx
  - src/components/admin/utilizatori/user-role-tabs.tsx
  - src/components/admin/utilizatori/user-profile-drawer.tsx
  - src/components/admin/utilizatori/registration-growth-chart.tsx
  - src/components/admin/utilizatori/invite-modal.tsx
  - src/components/admin/utilizatori/utilizatori-skeleton.tsx
autonomous: true
requirements:
  - UTL-01
  - UTL-02
  - UTL-03
  - UTL-04
  - UTL-05
must_haves:
  truths:
    - "Admin sees a user list with role filter tabs (Toți / Cetățeni / Funcționari / Primar / Admin) showing real counts from database"
    - "Admin can click any user row to open a profile drawer with full details (email, phone, department, last login, 2FA status, cereri count)"
    - "Admin can approve pending users, suspend active users, and reactivate suspended users via Server Actions"
    - "Admin can open an invite modal to invite staff with role selection and department field"
    - "Registration growth AreaChart renders from real user_primarii table data (30-day trend by role)"
    - "pnpm build succeeds with zero TypeScript errors"
  artifacts:
    - path: "src/components/admin/utilizatori/utilizatori-content.tsx"
      provides: "Client Component — tabs, search, user table, modals coordinator"
      exports: ["UtilizatoriContent"]
    - path: "src/components/admin/utilizatori/user-role-tabs.tsx"
      provides: "Role filter tabs with live counts"
      exports: ["UserRoleTabs"]
    - path: "src/components/admin/utilizatori/user-profile-drawer.tsx"
      provides: "Slide-in drawer with full user details + action buttons"
      exports: ["UserProfileDrawer"]
    - path: "src/components/admin/utilizatori/registration-growth-chart.tsx"
      provides: "30-day registration AreaChart from real DB data"
      exports: ["RegistrationGrowthChart"]
    - path: "src/components/admin/utilizatori/invite-modal.tsx"
      provides: "Invite staff modal with role + department fields"
      exports: ["InviteModal"]
    - path: "src/components/admin/utilizatori/utilizatori-skeleton.tsx"
      provides: "Loading skeleton matching content layout"
      exports: ["UtilizatoriSkeleton"]
  key_links:
    - from: "src/app/app/[judet]/[localitate]/admin/users/page.tsx"
      to: "src/components/admin/utilizatori/utilizatori-content.tsx"
      via: "Server Component passes users[] + growthData[] props"
      pattern: "UtilizatoriContent"
    - from: "utilizatori-content.tsx"
      to: "shared/role-color-badge.tsx"
      via: "import RoleColorBadge"
      pattern: "RoleColorBadge"
---

<objective>
Wave 1 — Full rewrite of the Utilizatori admin page to pixel-perfect Figma match with real Supabase data.

Purpose: Replace Phase 19 Utilizatori components (delete and rewrite) with a Figma-accurate UI: role tabs, searchable user table, profile drawer, invite modal, growth chart. All connected to real utilizatori + user_primarii tables.

Output: 7 files in src/components/admin/utilizatori/ + updated page.tsx. Users page shows real data from DB.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-CONTEXT.md
@.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-RESEARCH.md

<interfaces>
<!-- Figma source: Revamp Primarie Admin/src/app/components/pages/UtilizatoriPage.tsx (985 lines) -->
<!-- Key visual elements from Figma (adapt these to Next.js patterns):
  - Role filter tabs: Toți | Cetățeni | Funcționari | Primar | Admin (with count badges)
  - Search bar + sort dropdown in header area
  - User table rows: avatar circle, name+email, role badge, status badge, last active, action (...) menu
  - Clicking row opens profile drawer (right slide-in) with full user card
  - Profile drawer: avatar, name, role badge, status, tabs for Profile/Activity/Security info
  - Bottom of drawer: approve/suspend/reactivate action buttons
  - Invite Staff button in page header → opens InviteModal
  - InviteModal: email field, role select (functionar/primar only — admin cannot invite admin), department select
  - RegistrationGrowthChart: AreaChart for 30-day trend by role (from user_primarii table)
  - Role color config (use RoleColorBadge from shared/):
      cetatean: blue, functionar: emerald, primar: amber, admin: violet
  - Status config: activ=emerald, pending=amber, suspendat=red, inactiv=gray
-->

<!-- page.tsx already has the correct data fetch — keep it: -->
const [usersResult, growthResult] = await Promise.all([
  admin.from("utilizatori")
    .select("id, email, nume, prenume, rol, status, activ, departament, telefon, avatar_url, created_at, last_login_at, primarie_id")
    .eq("primarie_id", primarieId).order("created_at", { ascending: false }).limit(200),
  admin.from("user_primarii")
    .select("created_at, rol").eq("primarie_id", primarieId).gte("created_at", thirtyDaysAgo.toISOString()),
]);

<!-- UtilizatoriContent props interface: -->
interface UtilizatoriContentProps {
  users: Array<{
    id: string; email: string; nume: string; prenume: string;
    rol: string; status: string; activ: boolean | null;
    departament: string | null; telefon: string | null;
    avatar_url: string | null; created_at: string | null;
    last_login_at: string | null; primarie_id: string | null;
  }>;
  growthData: Array<{ created_at: string; rol: string }>;
  primarieId: string;
}

<!-- Server Action pattern for role/status mutations (use service role bypass RLS): -->
// src/components/admin/utilizatori/actions.ts (create this too)
"use server";
import { createServiceRoleClient } from "@/lib/supabase/server";
export async function updateUserRole(userId: string, newRole: string): Promise<{ error?: string }>;
export async function updateUserStatus(userId: string, newStatus: string): Promise<{ error?: string }>;

<!-- Recharts for growth chart — hex colors in stroke/fill (CSS vars don't work in SVG attributes): -->
// cetatean line: stroke="#3b82f6"
// functionar line: stroke="#10b981"
// primar line: stroke="#f59e0b"
// admin line: stroke="#8b5cf6"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Phase 19 utilizatori files + rewrite all 7 components</name>
  <files>
    src/components/admin/utilizatori/utilizatori-content.tsx
    src/components/admin/utilizatori/user-role-tabs.tsx
    src/components/admin/utilizatori/user-profile-drawer.tsx
    src/components/admin/utilizatori/registration-growth-chart.tsx
    src/components/admin/utilizatori/invite-modal.tsx
    src/components/admin/utilizatori/utilizatori-skeleton.tsx
    src/components/admin/utilizatori/actions.ts
  </files>
  <action>
    **Step 0 — Delete Phase 19 files:**
    ```bash
    rm -rf src/components/admin/utilizatori/
    mkdir -p src/components/admin/utilizatori/
    ```

    **Step 1 — Create actions.ts (Server Actions for mutations):**

    Create `src/components/admin/utilizatori/actions.ts`:
    - `"use server"` at top
    - Import `createServiceRoleClient`, `getAuthContext` from correct paths
    - `updateUserRole(userId: string, newRole: string, primarieId: string): Promise<{ error?: string }>` — updates utilizatori.rol using service role client; also updates user_primarii.rol if record exists; returns error string on failure
    - `updateUserStatus(userId: string, newStatus: "activ" | "suspendat" | "inactiv" | "pending", primarieId: string): Promise<{ error?: string }>` — updates utilizatori.status; returns error string on failure
    - Both actions call `revalidatePath("/app/[judet]/[localitate]/admin/users")` — but since judet/localitate are dynamic, just use `revalidatePath("/", "layout")` as the revalidation scope

    **Step 2 — Create utilizatori-skeleton.tsx:**

    Standard skeleton pattern: animated pulse boxes matching the page layout:
    - Title bar skeleton (h-8 w-48)
    - 5 tab skeleton pills in a row
    - 8 skeleton rows in a table (h-14 each with avatar circle + 3 text bars)
    - No logic, pure skeleton. Use `animate-pulse` and `bg-white/[0.06]` on all skeleton elements.

    **Step 3 — Create user-role-tabs.tsx:**

    Props: `{ activeRole: string; onRoleChange: (role: string) => void; counts: Record<string, number> }`

    Renders 5 tabs: "Toți" / "cetatean" / "functionar" / "primar" / "admin" with labels "Cetățeni" / "Funcționari" / "Primar" / "Admin".

    Each tab shows count badge. Active tab has `background: var(--accent-500)` with white text. Inactive tabs: `bg-white/[0.04]` with `text-gray-400`.

    Framer Motion: `<motion.div layoutId="role-tab-indicator">` for the active indicator.

    **Step 4 — Create registration-growth-chart.tsx:**

    Props: `{ growthData: Array<{ created_at: string; rol: string }> }`

    Aggregate growthData by week (group by YYYY-W key) and count by role.
    Use Recharts AreaChart (ResponsiveContainer, XAxis, YAxis, Tooltip, Area).
    4 Area components: cetatean (#3b82f6), functionar (#10b981), primar (#f59e0b), admin (#8b5cf6).
    SVG gradient defs per color. CustomTooltip uses `var(--card)` background and `var(--card-foreground)` text (not hardcoded `#1a1a2e`).
    Chart height: 180px. Show last 6 weeks max.

    **Step 5 — Create invite-modal.tsx:**

    Props: `{ open: boolean; onClose: () => void; primarieId: string }`

    AnimatePresence modal overlay (fixed inset-0 z-[100], bg-black/60 backdrop-blur-sm).
    Inner panel: `bg-card border border-border rounded-2xl p-6 max-w-md w-full`.
    Fields: email input, role select (options: functionar, primar — cannot invite admin), department select (8 options from Figma).
    Submit button: gradient using `var(--accent-gradient)`. On submit: `toast.success("Invitație trimisă!")` (mock — no actual email sending for academic milestone).
    Close via X button or backdrop click.

    **Step 6 — Create user-profile-drawer.tsx:**

    Props: `{ user: UtilizatoriUser | null; onClose: () => void; primarieId: string; onRoleUpdated: () => void; onStatusUpdated: () => void }`

    Slide-in from right: `fixed inset-y-0 right-0 w-96 z-[90] flex flex-col`.
    Framer Motion: `initial={{ x: "100%" }} animate={{ x: 0 }} exit={{ x: "100%" }}`.
    Content:
    - Avatar circle (first letter of prenume, colored by role)
    - Name, email, role badge (use RoleColorBadge from shared/), status badge
    - Info rows: phone, department, last_login_at, created_at, cereri count (mock "—" if no direct cereri count in prop)
    - Action buttons row at bottom:
      - "Aprobă" (if status === "pending") → calls updateUserStatus(id, "activ")
      - "Suspendă" (if status === "activ") → calls updateUserStatus(id, "suspendat")
      - "Reactivează" (if status === "suspendat") → calls updateUserStatus(id, "activ")
      - "Schimbă rol" → opens inline role select → calls updateUserRole
    - Each action shows toast.success/toast.error based on result
    - Backdrop overlay behind drawer catches click to close

    **Step 7 — Create utilizatori-content.tsx (coordinator):**

    `"use client"`. Receives `users`, `growthData`, `primarieId` from Server Component.

    State: `activeRole`, `search`, `page (1)`, `selectedUser (null)`, `showInvite (false)`.

    Layout:
    ```
    Page header: "Utilizatori" title + "Invită Staff" button (opens InviteModal)
    UserRoleTabs (role filter, with counts derived from users array)
    Search bar + sort dropdown
    RegistrationGrowthChart (collapsed by default, toggle button "Grafic Creștere")
    User table (8 per page, filtered by activeRole + search):
      - Avatar circle, Name+Email, RoleColorBadge, status pill, lastActive, (...) menu
      - Row click → opens UserProfileDrawer
    Pagination (prev/next + page count)
    UserProfileDrawer (AnimatePresence, portal via fixed positioning)
    InviteModal (AnimatePresence)
    ```

    All hex colors in the UI (card backgrounds, borders) → CSS tokens:
    - Card `bg-white/[0.025]` with `border border-white/[0.05]`
    - Table rows: `hover:bg-white/[0.03]`
    - Status pills: match Figma colors using Tailwind: `bg-emerald-500/15 text-emerald-500` etc.

    Framer Motion:
    - Page entry: `motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}`
    - Row list: `AnimatePresence mode="popLayout"` with `layout` on each row
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -E "error TS|Error:" | head -20 || echo "Build clean"</automated>
  </verify>
  <done>
    All 7 files created. pnpm build succeeds. UtilizatoriContent, UserRoleTabs, UserProfileDrawer, RegistrationGrowthChart, InviteModal, UtilizatoriSkeleton, actions all export correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update page.tsx to wire components + validate imports</name>
  <files>
    src/app/app/[judet]/[localitate]/admin/users/page.tsx
  </files>
  <action>
    The existing page.tsx at /admin/users/page.tsx already has the correct data fetch pattern from Phase 19. Review it and make these updates:

    1. Verify import paths: `UtilizatoriContent` from `@/components/admin/utilizatori/utilizatori-content` and `UtilizatoriSkeleton` from `@/components/admin/utilizatori/utilizatori-skeleton`. Update if paths changed.

    2. Verify `params` is awaited (`await params` — Next.js 15 requirement).

    3. Verify the `headers()` call for `x-primarie-id` header extraction — keep as-is.

    4. Verify `createServiceRoleClient()` is used for the data fetch (bypasses RLS for admin view).

    5. The page must pass exactly these props to UtilizatoriContent:
       ```typescript
       <UtilizatoriContent users={users} growthData={growthData} primarieId={primarieId} />
       ```

    6. If the page currently points to a different route (e.g., /admin/users but sidebar links to /admin/utilizatori), add a note in a comment but do NOT change the route — routing is out of scope for Phase 20.

    Make minimal changes — the page.tsx structure is already correct, just ensure import paths match the new files.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -E "error TS|Error:" | head -20 || echo "Build clean"</automated>
  </verify>
  <done>
    page.tsx imports UtilizatoriContent and UtilizatoriSkeleton from the new Phase 20 files. pnpm build green. No TypeScript errors.
  </done>
</task>

</tasks>

<verification>
After Wave 1:
- pnpm build passes with zero TypeScript errors
- All 7 utilizatori components created and exportable
- No any types in new files (use LucideIcon for icon props)
- Role color tokens from Wave 0 used via RoleColorBadge component
- Recharts SVG strokes use hex strings (not CSS vars) — confirmed anti-pattern avoided
- CustomTooltip uses var(--card) not hardcoded #1a1a2e
- All hardcoded dark backgrounds replaced with CSS tokens
</verification>

<success_criteria>
- Utilizatori page loads in browser showing real user data from DB
- Role filter tabs show correct counts per role
- Profile drawer slides in from right when user row is clicked
- Actions (approve/suspend) call Server Actions and show toast feedback
- Growth chart renders from user_primarii data
- Both light and dark themes render without hardcoded colors
</success_criteria>

<output>
After completion, create `.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-01-SUMMARY.md`
</output>
