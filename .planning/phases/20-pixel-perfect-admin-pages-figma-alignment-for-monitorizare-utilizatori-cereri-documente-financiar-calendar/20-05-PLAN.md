---
phase: 20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar
plan: "05"
type: execute
wave: 6
depends_on: ["20-04"]
files_modified:
  - src/app/app/[judet]/[localitate]/admin/calendar/page.tsx
  - src/components/admin/calendar/calendar-content.tsx
  - src/components/admin/calendar/calendar-grid.tsx
  - src/components/admin/calendar/event-detail-panel.tsx
  - src/components/admin/calendar/create-event-modal.tsx
  - src/components/admin/calendar/calendar-skeleton.tsx
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04
must_haves:
  truths:
    - "Admin sees a full month grid (Monday-start, 7 columns) with day cells, event dots, and today highlighted"
    - "Admin can click a day to see its events in the detail panel on the right"
    - "Admin can click 'Eveniment Nou' to open a modal and create an event (title, date, time, location, color)"
    - "Created events persist in Zustand store (survive navigation, lost on tab close — academic milestone)"
    - "Event color stored as Tailwind bg-* class name string (not hex)"
    - "pnpm build succeeds with zero TypeScript errors"
  artifacts:
    - path: "src/components/admin/calendar/calendar-content.tsx"
      provides: "Client Component coordinator for calendar page"
      exports: ["CalendarContent"]
    - path: "src/components/admin/calendar/calendar-grid.tsx"
      provides: "Month grid with day cells, event dots, today highlight, past-day opacity"
      exports: ["CalendarGrid"]
    - path: "src/components/admin/calendar/event-detail-panel.tsx"
      provides: "Right panel showing events for selected day + upcoming events list"
      exports: ["EventDetailPanel"]
    - path: "src/components/admin/calendar/create-event-modal.tsx"
      provides: "Modal for creating new calendar events with color picker"
      exports: ["CreateEventModal"]
    - path: "src/components/admin/calendar/calendar-skeleton.tsx"
      provides: "Loading skeleton for calendar page"
      exports: ["CalendarSkeleton"]
  key_links:
    - from: "calendar-content.tsx"
      to: "Zustand calendar events store"
      via: "useCalendarStore() hook — events persisted via Zustand + localStorage"
      pattern: "useCalendarStore"
    - from: "create-event-modal.tsx"
      to: "calendar-content.tsx"
      via: "onEventCreate callback → dispatches to Zustand store"
      pattern: "onEventCreate"
---

<objective>
Wave 5 — Full rewrite of the Calendar admin page to pixel-perfect Figma match with Zustand-only event persistence.

Purpose: Replace Phase 19 Calendar components (delete and rewrite) with Figma-accurate calendar: full month grid (Monday-start), event dots, day detail panel, and event creation modal. Events stored in Zustand (localStorage persistence) per academic milestone decision.

Output: 5 files in src/components/admin/calendar/ + page.tsx stays as-is (already correct). Zustand calendar store created or verified to exist.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-CONTEXT.md
@.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-RESEARCH.md

<interfaces>
<!-- Figma source: Revamp Primarie Admin/src/app/components/pages/CalendarPage.tsx (264 lines) -->
<!-- Key Figma visual elements:
  - Header: "Calendar" title (CalendarDays sky icon), "Evenimente, deadlines și ședințe programate"
  - "Eveniment Nou" button (blue-indigo gradient)
  - Grid layout: col-span-8 for calendar + col-span-4 for detail panel
  - Calendar grid: 7-col grid, Mon-Sun headers, day cells 60px min-height
  - Today cell: pink gradient background, pink text
  - Selected cell: bg-white/[0.04], accent ring
  - Past days: opacity-40
  - Event dots: colored circles (1.5x1.5 rounded-full) per event, max 3 shown
  - Detail panel: Selected day events list + "Upcoming Events" list below
  - Event card in detail: colored dot + title + time + location + type
  - Create event modal: title, date, time, location (optional), color picker (6 color options)
  - Color picker: 6 colored circles, selected has ring; stores Tailwind class (e.g., "bg-pink-500")
-->

<!-- Zustand calendar store (check if exists first, create if not): -->
// Check: src/store/calendar-store.ts or src/store/use-calendar-store.ts
// If not found, create it:
import { create } from "zustand";
import { persist } from "zustand/middleware";

interface CalEvent {
  id: string;
  title: string;
  date: string;    // ISO date string "2026-03-15"
  time: string;    // "HH:MM"
  color: string;   // Tailwind class string: "bg-pink-500", "bg-red-500" etc.
  type: string;    // event type label
  location?: string;
}

interface CalendarStore {
  events: CalEvent[];
  addEvent: (event: Omit<CalEvent, "id">) => void;
  removeEvent: (id: string) => void;
}

const useCalendarStore = create<CalendarStore>()(
  persist(
    (set) => ({
      events: [
        // Initial mock events matching Figma reference (Martie 2026 data)
        { id: "e1", title: "Ședință Consiliu Local", date: "2026-03-04", time: "10:00", color: "bg-pink-500", type: "Ședință", location: "Sala Mare" },
        { id: "e2", title: "Deadline cerere #1839", date: "2026-03-05", time: "17:00", color: "bg-red-500", type: "Deadline" },
        { id: "e3", title: "Audit documente Q1", date: "2026-03-07", time: "09:00", color: "bg-amber-500", type: "Audit", location: "Biroul 3" },
        { id: "e4", title: "Întâlnire contribuabili", date: "2026-03-10", time: "14:00", color: "bg-blue-500", type: "Întâlnire", location: "Sala Conferințe" },
        { id: "e5", title: "Review buget lunar", date: "2026-03-12", time: "11:00", color: "bg-violet-500", type: "Review" },
        { id: "e6", title: "Training funcționari noi", date: "2026-03-14", time: "09:30", color: "bg-emerald-500", type: "Training", location: "Sala IT" },
      ],
      addEvent: (event) => set((state) => ({ events: [...state.events, { ...event, id: crypto.randomUUID() }] })),
      removeEvent: (id) => set((state) => ({ events: state.events.filter((e) => e.id !== id) })),
    }),
    { name: "primariata-calendar-events" }
  )
);

export { useCalendarStore };
export type { CalEvent };

<!-- Calendar grid math (from Figma CalendarPage): -->
// March 2026: daysInMonth=31, firstDayOffset=6 (Sunday → Mon-start grid offset 6)
// Dynamic: compute from current year/month using Date
function getMonthCalendar(year: number, month: number): (number | null)[] {
  const daysInMonth = new Date(year, month + 1, 0).getDate();
  const firstDayOfMonth = new Date(year, month, 1).getDay();
  // Convert Sunday=0 to Monday-start: (0→6, 1→0, 2→1, ..., 6→5)
  const offset = (firstDayOfMonth + 6) % 7;
  const days: (number | null)[] = [];
  for (let i = 0; i < offset; i++) days.push(null);
  for (let i = 1; i <= daysInMonth; i++) days.push(i);
  while (days.length % 7 !== 0) days.push(null);
  return days;
}

<!-- Month navigation: allow going to prev/next month (Figma has prev/next chevrons) -->
<!-- Today button: sets month+year to current system date -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Phase 19 calendar files + create Zustand store + skeleton + calendar grid</name>
  <files>
    src/components/admin/calendar/calendar-skeleton.tsx
    src/components/admin/calendar/calendar-grid.tsx
    src/store/calendar-store.ts
  </files>
  <action>
    **Step 0 — Delete Phase 19 files:**
    ```bash
    rm -rf src/components/admin/calendar/
    mkdir -p src/components/admin/calendar/
    ```

    **Step 1 — Check/Create Zustand calendar store:**

    First check if `src/store/calendar-store.ts` or similar already exists:
    ```bash
    ls src/store/ 2>/dev/null
    ```

    If a calendar store already exists with CalEvent + useCalendarStore, verify its shape matches the interface above. If it stores hex colors, UPDATE to store Tailwind class strings ("bg-pink-500" etc.) per Phase 19 decision.

    If not found: create `src/store/calendar-store.ts` with the full implementation shown in the interfaces section above. Use `crypto.randomUUID()` for event IDs. Persist with Zustand persist middleware under key `"primariata-calendar-events"`.

    **Step 2 — Create calendar-skeleton.tsx:**

    Animated pulse skeleton: header skeleton + grid-cols-12 layout (col-8 calendar grid + col-4 panel). Calendar grid: 7-col header row + 6 rows of 7 day cells (each 60px h). Panel: 2 stacked card skeletons. Use `bg-white/[0.06] animate-pulse rounded-xl`.

    **Step 3 — Create calendar-grid.tsx:**

    Props:
    ```typescript
    interface CalendarGridProps {
      year: number;
      month: number;
      events: CalEvent[];
      selectedDay: number | null;
      onDaySelect: (day: number) => void;
    }
    ```

    Compute `calendarDays` using `getMonthCalendar(year, month)`.

    Month name in Romanian (hardcode lookup or use `Intl.DateTimeFormat("ro-RO", { month: "long" }).format(new Date(year, month))`).

    Render:
    - Card wrapper: `bg-white/[0.025] border border-white/[0.05] rounded-2xl p-5`
    - Month nav bar: `{monthName} {year}` + prev/next chevron buttons (emit onMonthChange callback) + "Azi" button
    - Day headers row (Lun/Mar/Mie/Joi/Vin/Sâm/Dum): `text-gray-500 text-xs uppercase`
    - Day cells grid:
      - Empty cells: `<div key={...} />`
      - Day button: `motion.button whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}`
      - Today: `background: "linear-gradient(135deg, rgba(236,72,153,0.15), rgba(139,92,246,0.1))"` + `text-pink-400 font-bold`
        NOTE: This inline style gradient is OK — it's a non-SVG CSS gradient, not a hex color token violation
      - Selected: `bg-white/[0.04] ring-1 ring-accent-500/40`
      - Past day: `opacity-40`
      - Event dots: up to 3 per day, `w-1.5 h-1.5 rounded-full` with event.color (Tailwind class, so use className={event.color})
    - Min height per cell: 60px
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -E "error TS|Error:" | head -10 || echo "Build clean"</automated>
  </verify>
  <done>
    calendar-skeleton, calendar-grid, calendar-store (Zustand) created. pnpm build passes. Events stored as Tailwind class strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create event-detail-panel + create-event-modal + calendar-content coordinator</name>
  <files>
    src/components/admin/calendar/event-detail-panel.tsx
    src/components/admin/calendar/create-event-modal.tsx
    src/components/admin/calendar/calendar-content.tsx
  </files>
  <action>
    **Step 1 — Create event-detail-panel.tsx:**

    Props: `{ selectedDay: number | null; year: number; month: number; events: CalEvent[]; onEventRemove: (id: string) => void }`

    Layout: `col-span-4 flex flex-col gap-4`

    Card 1 — "Events for selected day":
    - Header: `{selectedDay} {monthName} {year}` + count badge
    - AnimatePresence on event list
    - Per event: colored left border `(1px solid event.color as actual style)` — actually: use `style={{ background: "..." }}` based on Tailwind class. But since `event.color` is "bg-pink-500", extract the color via inline style map:
      ```typescript
      const dotColorMap: Record<string, string> = {
        "bg-pink-500": "#ec4899", "bg-red-500": "#ef4444", "bg-amber-500": "#f59e0b",
        "bg-blue-500": "#3b82f6", "bg-violet-500": "#8b5cf6", "bg-emerald-500": "#10b981",
        "bg-cyan-500": "#06b6d4",
      };
      ```
      Use `dotColorMap[event.color] ?? "#6b7280"` for the dot and card accent. This is the EVENT DOT only (a presentational color dot, not a chart SVG) — acceptable hex usage.
    - Per event card: title, time (Clock icon), location (MapPin icon, optional), type (CalendarDays icon)
    - Remove button (X icon) on hover → `onEventRemove(event.id)` + `toast.success("Eveniment șters")`
    - Empty state: CalendarDays icon + "Niciun eveniment" text

    Card 2 — "Upcoming Events":
    - Filter events where date >= today (ISO date comparison)
    - Sort by date asc, take first 5
    - Each: colored vertical bar, title, date + time text
    - Clicking → `onDaySelect(event.date.getDate())` (parse date string to get day number)

    **Step 2 — Create create-event-modal.tsx:**

    Props: `{ open: boolean; onClose: () => void; onEventCreate: (event: Omit<CalEvent, "id">) => void }`

    AnimatePresence modal (fixed inset-0 z-[100] backdrop blur).
    Inner panel: `bg-card border border-border rounded-2xl p-6 max-w-md w-full`

    Fields (use RHF or simple useState):
    - Title input (required)
    - Date input (type="date", colorScheme: "dark" for input widget dark mode)
    - Time input (type="time")
    - Location input (optional)
    - Type input (text, default "Ședință")
    - Color picker: 7 colored circle buttons (pink, red, amber, blue, violet, emerald, cyan)
      - Each renders as `<button className={`w-8 h-8 rounded-full ${color} cursor-pointer transition-all`}>`
      - Selected: `ring-2 ring-white ring-offset-2 ring-offset-card`
      - Stores Tailwind class string ("bg-pink-500") — not hex
    - Submit button: accent gradient + "Salvează Evenimentul" label

    On submit: validates title + date not empty, calls `onEventCreate({ title, date, time, color, type, location })`, toast.success, onClose.

    **Step 3 — Create calendar-content.tsx (coordinator):**

    `"use client"`. No props needed (Zustand provides events directly).

    ```typescript
    const { events, addEvent, removeEvent } = useCalendarStore();
    ```

    State: `selectedDay: number | null`, `currentYear: number`, `currentMonth: number`, `showCreateModal: boolean`.

    Initialize year/month to current system date (`new Date()` — do NOT hardcode March 2026).

    Month navigation: `prevMonth` / `nextMonth` handlers that handle year rollover:
    - If month goes < 0 → year-1, month=11
    - If month goes > 11 → year+1, month=0

    Layout: `grid grid-cols-12 gap-5`

    Header (full width): "Calendar" title + "Eveniment Nou" button (sky/blue gradient).

    ```tsx
    <CalendarGrid
      year={currentYear}
      month={currentMonth}
      events={events.filter(e => /* e.date matches current month+year */)}
      selectedDay={selectedDay}
      onDaySelect={setSelectedDay}
      onMonthChange={(dir) => dir === "prev" ? prevMonth() : nextMonth()}
    />
    <EventDetailPanel
      selectedDay={selectedDay}
      year={currentYear}
      month={currentMonth}
      events={events}
      onEventRemove={removeEvent}
    />
    <CreateEventModal
      open={showCreateModal}
      onClose={() => setShowCreateModal(false)}
      onEventCreate={(e) => { addEvent(e); toast.success("Eveniment adăugat!"); }}
    />
    ```

    Filter events for CalendarGrid: only events where `e.date.startsWith(\`${year}-${String(month+1).padStart(2,"0")}\`)`

    Framer Motion: `motion.div` page entry animation.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -E "error TS|Error:" | head -10 || echo "Build clean"</automated>
  </verify>
  <done>
    event-detail-panel, create-event-modal, calendar-content created. pnpm build green. Events stored as Tailwind class strings. Month navigation handles year rollover. useCalendarStore provides events.
  </done>
</task>

</tasks>

<verification>
After Wave 5:
- pnpm build passes with zero TypeScript errors
- Calendar page loads with current month (not hardcoded March 2026)
- Initial mock events appear as dots in the grid
- Creating a new event via modal adds it to the grid immediately (Zustand reactive)
- Event details panel shows events for the selected day
- Upcoming events list shows future events sorted by date
- Month navigation (prev/next) works with year rollover
- Event color is stored as Tailwind class string ("bg-pink-500"), not hex
</verification>

<success_criteria>
- Admin can create an event and see it appear on the calendar grid immediately
- Events persist if admin navigates away and comes back (Zustand + localStorage)
- Selected day detail panel shows correct events
- Both light and dark themes render correctly
- pnpm build green
</success_criteria>

<output>
After completion, create `.planning/phases/20-pixel-perfect-admin-pages-figma-alignment-for-monitorizare-utilizatori-cereri-documente-financiar-calendar/20-05-SUMMARY.md`
</output>
