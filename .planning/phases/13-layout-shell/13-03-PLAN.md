---
phase: 13-layout-shell
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/layout.tsx
  - src/app/admin/providers.tsx
  - src/app/app/[judet]/[localitate]/layout.tsx
  - src/app/app/[judet]/[localitate]/providers.tsx
  - src/middleware.ts
  - src/components/shell/ShellLayout.tsx
  - src/components/shell/sidebar/sidebar-config.ts
autonomous: true
gap_closure: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-06, SHELL-07, SHELL-08, SEC-01, SEC-02]

must_haves:
  truths:
    - "Admin Primarie shell renders at /app/[judet]/[localitate]/admin/* with correct sidebar config"
    - "Survey Admin at /admin/* uses the original DashboardSidebar/DashboardHeader layout (not ShellLayout)"
    - "Middleware enforces admin role for /app/[judet]/[localitate]/admin/* paths instead of /admin/*"
    - "Non-admin users navigating to admin paths under /app/[judet]/[localitate]/admin/* are redirected"
  artifacts:
    - path: "src/app/app/[judet]/[localitate]/layout.tsx"
      provides: "Layout that detects admin sub-path and applies admin shell config"
    - path: "src/app/admin/layout.tsx"
      provides: "Reverted Survey Admin layout using DashboardSidebar/DashboardHeader"
    - path: "src/middleware.ts"
      provides: "Admin role enforcement on /app/*/admin/* instead of /admin/*"
  key_links:
    - from: "src/app/app/[judet]/[localitate]/providers.tsx"
      to: "src/components/shell/sidebar/sidebar-config.ts"
      via: "getAdminSidebarConfig(basePath) where basePath = /app/{judet}/{localitate}/admin"
      pattern: "getAdminSidebarConfig"
    - from: "src/middleware.ts"
      to: "user_primarii table"
      via: "role check for admin sub-paths"
      pattern: "admin.*user_primarii"
---

<objective>
Fix BLOCKER: Move admin primarie shell from /admin/* (Survey Admin route) to /app/[judet]/[localitate]/admin/* per architecture doc. Revert /admin/ layout to original Survey Admin DashboardSidebar/DashboardHeader pattern.

Purpose: The admin shell was placed on the wrong route, overwriting the completed Survey Admin (M7). Per docs/03-Arhitectura/Admin-Hierarchy-RO.md, Admin Primarie = /app/[judet]/[localitate]/admin/, Survey Admin = /admin/survey/*.
Output: Admin shell at correct route, Survey Admin restored, middleware enforcement updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-layout-shell/13-UAT.md

@docs/03-Arhitectura/Admin-Hierarchy-RO.md (route hierarchy reference)
@src/middleware.ts (current middleware with /admin/* enforcement)
@src/app/admin/layout.tsx (current -- incorrectly uses ShellLayout)
@src/app/admin/providers.tsx (current -- AdminProviders with ShellLayout)
@src/app/app/[judet]/[localitate]/layout.tsx (citizen layout using ShellLayout)
@src/app/app/[judet]/[localitate]/providers.tsx (citizen providers pattern)
@src/components/shell/sidebar/sidebar-config.ts (getAdminSidebarConfig, getCitizenSidebarConfig)
@src/components/shell/ShellLayout.tsx (ShellLayout component)
@src/components/dashboard/DashboardSidebar.tsx (original sidebar for Survey Admin)
@src/components/dashboard/DashboardHeader.tsx (original header for Survey Admin)

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/shell/sidebar/sidebar-config.ts:
```typescript
export interface SidebarConfig {
  role: string;
  roleLabel: string;
  sections: NavSection[];
}
export function getAdminSidebarConfig(basePath: string): SidebarConfig;
export function getCitizenSidebarConfig(basePath: string): SidebarConfig;
```

From src/components/shell/ShellLayout.tsx:
```typescript
interface ShellLayoutProps {
  children: React.ReactNode;
  sidebarConfig: SidebarConfig;
  initialCollapsed: boolean;
}
```

From src/app/app/[judet]/[localitate]/providers.tsx:
```typescript
// Pattern to follow for admin providers:
interface CitizenProvidersProps {
  children: React.ReactNode;
  sidebarConfig: SidebarConfig;
  initialCollapsed: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert /admin/ layout and move admin shell to /app/[judet]/[localitate]/admin/</name>
  <files>
    src/app/admin/layout.tsx,
    src/app/admin/providers.tsx,
    src/app/app/[judet]/[localitate]/layout.tsx,
    src/app/app/[judet]/[localitate]/providers.tsx,
    src/components/shell/ShellLayout.tsx
  </files>
  <action>
    **Part A: Revert /admin/layout.tsx to original Survey Admin layout.**

    Restore the original DashboardSidebar/DashboardHeader pattern from before Phase 13. The layout should be a "use client" component with:
    - DashboardSidebar with admin-specific navigation links: Global Admin (/admin/primariata), Survey Platform (/admin/survey), Setari Admin (/admin/settings)
    - DashboardHeader simplified (no location/weather)
    - QueryClientProvider wrapping
    - Standard mobile/resize detection
    - Remove the ShellLayout usage entirely from /admin/

    Delete src/app/admin/providers.tsx (no longer needed -- the layout handles providers inline as it did before Phase 13).

    **Part B: Update citizen layout to detect admin sub-path and apply admin shell config.**

    The /app/[judet]/[localitate]/admin/* pages are NESTED inside the citizen layout. The citizen layout at src/app/app/[judet]/[localitate]/layout.tsx already wraps everything in ShellLayout with citizen config. We need it to detect when the path is /admin/* and switch to admin config.

    Modify src/app/app/[judet]/[localitate]/layout.tsx:
    - Import both `getCitizenSidebarConfig` and `getAdminSidebarConfig`
    - Read `params` (judet, localitate) as currently done
    - Also check if the path contains `/admin` by examining the URL or using a different approach:
      - Since this is a Server Component layout, we cannot use `usePathname()`.
      - Instead: Create a SEPARATE layout file at `src/app/app/[judet]/[localitate]/admin/layout.tsx` that wraps admin children with the admin shell config.
      - The existing citizen layout stays as-is for non-admin paths.

    Actually, the CORRECT approach per Next.js App Router nested layouts:
    - Keep `src/app/app/[judet]/[localitate]/layout.tsx` as-is (citizen shell)
    - Create `src/app/app/[judet]/[localitate]/admin/layout.tsx` as a NEW server layout that overrides the parent shell for admin pages

    BUT: Since the citizen layout wraps ALL children including /admin/ children in ShellLayout with citizen config, we'd get NESTED shells (citizen shell wrapping admin shell). To avoid this:

    Option: Make the citizen layout ONLY wrap non-admin children. Check if the path includes /admin/ and if so, skip ShellLayout and just render children directly. The admin sub-layout will provide its own ShellLayout.

    Implementation:
    1. In `src/app/app/[judet]/[localitate]/layout.tsx`: This already wraps everything in CitizenProviders (which includes ShellLayout). We need to change this so the admin sub-route gets its own layout without double-nesting.

    The cleanest solution: Use a Next.js route group to separate admin and non-admin layouts under /app/[judet]/[localitate]/. But route groups would require moving all existing pages, which is too disruptive.

    Practical approach: Create `src/app/app/[judet]/[localitate]/admin/layout.tsx` as a NEW layout. Since Next.js applies layouts hierarchically, admin pages will get BOTH the parent citizen layout AND their own admin layout. To prevent double-shell:
    - The parent layout at `src/app/app/[judet]/[localitate]/layout.tsx` should detect if the request is for an admin sub-path and, if so, render children WITHOUT the ShellLayout wrapper (just pass children through with minimal wrapping -- QueryProvider only, no ShellLayout).
    - To detect admin path in a Server Component: use `headers()` to read the request URL, or better yet, create a separate admin layout that provides its own shell.

    FINAL APPROACH (simplest, proven pattern):
    1. Keep citizen layout as-is -- it wraps everything in CitizenProviders which includes ShellLayout.
    2. Create `src/app/app/[judet]/[localitate]/admin/layout.tsx` that does NOT add another ShellLayout (the citizen one already provides the shell).
    3. BUT the citizen shell has citizen nav items, not admin ones.

    CORRECT FINAL APPROACH:
    1. Modify `src/app/app/[judet]/[localitate]/layout.tsx` to check cookies or headers for path info. Since `headers()` is available in server components:
       - `const headersList = await headers(); const pathname = headersList.get("x-pathname") || "";`
       - But x-pathname is not set by default. Use `headersList.get("x-invoke-path")` or middleware-set header.
       - SIMPLEST: Set a header in middleware for admin sub-paths.

    ACTUALLY SIMPLEST APPROACH:
    Use Next.js route groups. Restructure:
    ```
    src/app/app/[judet]/[localitate]/
      (citizen)/          # Route group for citizen shell
        layout.tsx        # Citizen ShellLayout
        page.tsx          # Dashboard
        cereri/
        documente/
        ...
      (admin)/            # Route group for admin shell
        layout.tsx        # Admin ShellLayout
        admin/
          page.tsx
          registrations/
          settings/
          users/
    ```

    BUT THIS WOULD CHANGE ALL EXISTING CITIZEN URLS. Too disruptive.

    REAL SIMPLEST APPROACH:
    Since the existing citizen layout at `/app/[judet]/[localitate]/layout.tsx` wraps children in providers and ShellLayout, and we need admin children to get a DIFFERENT shell:

    1. Move ShellLayout selection INTO providers.tsx using `usePathname()` (client-side). The providers.tsx is already a "use client" component.
    2. In `src/app/app/[judet]/[localitate]/providers.tsx`:
       - Import `usePathname` from `next/navigation`
       - If `pathname.includes('/admin')`, use `getAdminSidebarConfig(basePath + '/admin')`
       - Otherwise, use citizen config
       - This is a single-file change with no route restructuring

    THIS IS THE APPROACH TO USE. Modify `src/app/app/[judet]/[localitate]/providers.tsx`:
    - Add `usePathname()` import
    - Compute `isAdmin = pathname.includes('/admin')`
    - Use `getAdminSidebarConfig(basePath + '/admin')` when isAdmin, else existing citizen config
    - Pass the correct config to ShellLayout

    Also update `src/app/app/[judet]/[localitate]/layout.tsx`:
    - Remove the `getCitizenSidebarConfig` import and call (move this to providers.tsx)
    - Pass `basePath` to CitizenProviders instead of `sidebarConfig`

    Wait -- the current layout passes `sidebarConfig` (which contains Lucide icon string names, so it IS serializable). But we need providers to choose the config dynamically. So:
    - Layout passes `basePath` to providers
    - Providers uses `usePathname` to decide admin vs citizen config
    - Providers calls the appropriate config function client-side

    Update src/components/shell/ShellLayout.tsx:
    - The `basePath` for CommandPalette is currently hardcoded: `basePath={sidebarConfig.role === "admin" ? "/admin" : ""}`. This needs to use the actual basePath. Add a `basePath` prop to ShellLayout or derive it from sidebarConfig. Simplest: add an optional `basePath` field to SidebarConfig.

    Add `basePath` field to SidebarConfig interface in sidebar-config.ts. Set it in getAdminSidebarConfig and getCitizenSidebarConfig. Use it in ShellLayout for CommandPalette basePath instead of hardcoded check.
  </action>
  <verify>
    <automated>cd /Users/thor/Documents/GitHub/primariata.work && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - /admin/layout.tsx uses DashboardSidebar/DashboardHeader (original Survey Admin pattern)
    - /admin/providers.tsx deleted
    - /app/[judet]/[localitate]/providers.tsx dynamically selects admin or citizen sidebar config based on pathname
    - SidebarConfig has basePath field, used in ShellLayout for CommandPalette
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update middleware admin enforcement for /app/[judet]/[localitate]/admin/* paths</name>
  <files>
    src/middleware.ts
  </files>
  <action>
    Update the admin role enforcement block in middleware.ts:

    1. **Remove the /admin/* enforcement for ShellLayout** (lines ~105-155 in current middleware). The `isAdminRoute` check for `/admin` should ONLY handle Survey Admin auth (redirect unauthenticated to /admin/login). It should NOT check for admin role in user_primarii -- Survey Admin is accessible to both admin and super_admin per the architecture doc, and this is already handled by Survey Admin's own auth.

    2. **Add admin enforcement for /app/*/admin/* paths.** In the existing primarie context resolution block (after `appRouteMatch && user`), add a check:
       - After the primarie is resolved and association validated, check if `pathAfterLocalitate.startsWith("/admin")`
       - If so, verify the user's association has `rol` of `admin` (or `primar` -- they may also need access)
       - If the user's role at this primarie is NOT admin, redirect to the citizen dashboard at the same primarie
       - Use the already-fetched `association.rol` from the existing defense-in-depth check (line ~193 current middleware)

    3. **Keep the existing /admin/* unauthenticated redirect** (isAdminRoute && !user -> /admin/login). This protects Survey Admin.

    4. **Remove the admin role check** from the /admin/* block (isAdminRoute && user). Survey Admin access control is handled by its own pages. The middleware should only ensure the user is authenticated for /admin/*.

    Concrete changes:
    - In the `isAdminRoute && user` block (lines ~115-155): Remove the entire serviceClient creation and user_primarii check. This block should just do nothing (let the request through since user is authenticated).
    - In the primarie context resolution block, after `association` is validated, add:
      ```typescript
      // Admin sub-path enforcement (SEC-01, SEC-02)
      if (pathAfterLocalitate.startsWith("/admin") && association.rol !== "admin") {
        const url = request.nextUrl.clone();
        url.pathname = `/app/${judetSlug}/${localitateSlug}`;
        return redirectWithCookies(url, supabaseResponse);
      }
      ```
    - This goes right after the existing `protectedModules` check (which already blocks unapproved users from /admin), but BEFORE the cross-primarie guard.
  </action>
  <verify>
    <automated>cd /Users/thor/Documents/GitHub/primariata.work && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Middleware no longer checks admin role for /admin/* (Survey Admin access)
    - Middleware enforces admin role for /app/[judet]/[localitate]/admin/* paths
    - Non-admin users at a primarie are redirected to citizen dashboard when accessing /admin sub-paths
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `pnpm type-check` passes
2. `pnpm build` succeeds
3. Admin Primarie shell renders at /app/[judet]/[localitate]/admin/ with admin sidebar config
4. Survey Admin at /admin/survey/ renders with original DashboardSidebar layout
5. Non-admin users redirected from /app/[judet]/[localitate]/admin/ to citizen dashboard
</verification>

<success_criteria>
- Admin shell is at /app/[judet]/[localitate]/admin/* with correct nav items (Dashboard, Monitorizare, Utilizatori, Cereri, Documente, Financiar, Calendar, Configurare)
- Survey Admin at /admin/* uses original DashboardSidebar/DashboardHeader layout
- Middleware admin role enforcement protects /app/[judet]/[localitate]/admin/* paths
- No double-shell nesting (admin pages have ONE shell, not citizen + admin)
</success_criteria>

<output>
After completion, create `.planning/phases/13-layout-shell/13-03-SUMMARY.md`
</output>
