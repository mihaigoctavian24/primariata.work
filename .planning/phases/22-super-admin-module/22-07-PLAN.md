---
phase: 22-super-admin-module
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/primariata/_components/super-admin-shell.tsx
  - src/app/admin/primariata/_components/super-admin-sidebar.tsx
  - src/app/admin/primariata/_components/super-admin-topbar.tsx
  - src/app/admin/primariata/_components/sa-dashboard-content.tsx
  - src/app/admin/primariata/_components/sa-primarii-content.tsx
  - src/app/admin/primariata/_components/sa-admins-content.tsx
  - src/app/admin/primariata/_components/sa-audit-content.tsx
  - src/app/admin/primariata/_components/sa-analytics-content.tsx
  - src/app/admin/primariata/_components/sa-settings-content.tsx
  - src/app/admin/primariata/_components/dashboard-kpis.tsx
  - src/app/admin/primariata/_components/platform-stats-charts.tsx
  - src/app/admin/primariata/_components/primarii-grid.tsx
  - src/app/admin/primariata/_components/primarii-table.tsx
  - src/app/admin/primariata/_components/primarii-status-list.tsx
  - src/app/admin/primariata/_components/admins-grid.tsx
  - src/app/admin/primariata/_components/admins-table.tsx
  - src/app/admin/primariata/_components/admin-detail-drawer.tsx
  - src/app/admin/primariata/_components/primarie-detail-drawer.tsx
  - src/app/admin/primariata/_components/pagination.tsx
  - src/app/admin/primariata/_components/glass-tooltip.tsx
  - src/app/admin/primariata/_components/recent-activity-list.tsx
  - src/app/admin/primariata/_components/admins-shared.tsx
  - src/app/admin/primariata/_components/primarii-shared.tsx
autonomous: true
requirements:
  - SA-THEME-01

must_haves:
  truths:
    - "Super admin module renders correctly in both light and dark themes without hardcoded dark-only hex colors"
    - "No hardcoded hex (#xxxxxx) or rgba() color values remain in super admin shell, sidebar, topbar, or any sa-*/primarii-*/admins-*/drawer component (exception: Recharts stroke/fill colors in CHART_COLORS constant)"
    - "All surfaces, borders, and text use CSS custom property tokens from Phase 12 globals.css"
    - "The shell background uses var(--background) instead of hardcoded #09090f"
    - "Recharts chart stroke/fill colors use a CHART_COLORS constant with semantic hex values (not CSS vars)"
  artifacts:
    - path: "src/app/admin/primariata/_components/super-admin-shell.tsx"
      provides: "Shell background using var(--background), borders using var(--border-subtle)"
    - path: "src/app/admin/primariata/_components/super-admin-sidebar.tsx"
      provides: "Sidebar using sidebar-background token, active state using accent tokens"
    - path: "src/app/admin/primariata/_components/super-admin-topbar.tsx"
      provides: "Topbar border using var(--border-subtle), badge backgrounds using accent tokens"
  key_links:
    - from: "super-admin-shell.tsx"
      to: "globals.css --background token"
      via: "style={{ background: 'var(--background)' }} replacing #09090f"
      pattern: "--background"
    - from: "super-admin-sidebar.tsx"
      to: "globals.css --sidebar-background token"
      via: "style={{ background: 'var(--sidebar-background)' }}"
      pattern: "--sidebar-background"
---

<objective>
Replace all hardcoded hex/rgba colors across the entire super admin module with Phase 12 CSS design tokens, enabling proper light and dark theme support.

Purpose: The super admin module is currently hardcoded to a dark-only visual — `#09090f` backgrounds, `rgba(255,255,255,0.04)` borders, `rgba(16,185,129,0.08)` accent tints. These values must be replaced with the existing CSS custom property system from Phase 12 so the module respects the theme toggle like the rest of the app.

Output: Every file in the super admin `_components/` folder with hardcoded hex/rgba replaced by CSS variable references. Recharts colors remain as a `CHART_COLORS` constant with semantic hex values.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-super-admin-module/22-CONTEXT.md

<interfaces>
<!-- Token mapping reference. Executor should use these substitutions systematically. -->

Phase 12 CSS tokens (from src/app/globals.css) — light and dark values both defined:

BACKGROUND / SURFACE tokens:
- `#09090f` (shell bg) → `var(--background)`
- `linear-gradient(180deg, #0c0c18, #08080f)` (sidebar bg) → `var(--sidebar-background)` (flat, no gradient needed)
- `linear-gradient(180deg, #0f0f1a, #09090f)` (drawer bg) → `var(--background)` or `var(--popover)`
- `rgba(255,255,255,0.03)` / `rgba(255,255,255,0.04)` (card/panel bg) → `var(--surface-raised)` or `var(--muted)`
- `rgba(255,255,255,0.025)` (section card bg) → `var(--muted)`
- `rgba(14,14,28,0.95)` (drawer backdrop) → `var(--popover)`
- `rgba(0,0,0,0.5)` (overlay backdrop) → `rgba(0,0,0,0.5)` — OK to keep (backdrop overlay, not a semantic token)

BORDER tokens:
- `rgba(255,255,255,0.04)` / `rgba(255,255,255,0.05)` / `rgba(255,255,255,0.06)` / `rgba(255,255,255,0.08)` (borders) → `var(--border-subtle)`
- `1px solid rgba(255,255,255,0.04)` → `1px solid var(--border-subtle)`
- `border-white/[0.04]` Tailwind class → replace with `border-[color:var(--border-subtle)]` or use `style={{ border: "1px solid var(--border-subtle)" }}`

TEXT tokens:
- `text-white` / `text-gray-200` / `text-gray-300` (primary text) → `text-foreground` / `var(--foreground)`
- `text-gray-500` / `text-gray-600` (muted text) → `text-muted-foreground` / `var(--muted-foreground)`
- `text-gray-400` (medium muted) → `text-muted-foreground`
- `color: "#fff"` inline → `color: var(--foreground)` — but avoid this; prefer Tailwind classes

ACCENT (emerald/cyan brand colors — the super admin module uses these as brand identity, keep them):
- `#10b981` (emerald-500), `#06b6d4` (cyan-400), `rgba(16,185,129,0.08)` accent tints → KEEP as-is OR map:
  - `#10b981` → `var(--accent-500)` (since accent-hue defaults to crimson but is runtime-overridable — this will change with user's accent choice)
  - Decision from CONTEXT.md: "Replace ALL hardcoded hex/rgba with project CSS design tokens"
  - However, the super admin module uses emerald/cyan as brand identity (not as accent color) — these should stay as semantic emerald/cyan colors
  - Safe approach: Replace generic surface/border/text colors with tokens. Keep emerald/cyan as brand palette constants defined at file level:
    ```typescript
    const SA_BRAND = { emerald: "#10b981", cyan: "#06b6d4" } as const;
    ```
  - This is Claude's discretion — the CONTEXT.md says chart colors exception uses CHART_COLORS; apply the same principle to brand identity colors in the super admin module

CHART COLORS (DO NOT change — keep as semantic hex palette):
- All Recharts `stroke`, `fill`, `color` props that are hex values → keep as-is in CHART_COLORS constant
- Example: `color: "#10b981"` on `<Line stroke="#10b981">` → keep as CHART_COLORS.emerald

PRACTICAL RULE for executor:
1. Replace ALL `style={{ background: "rgba(...)" }}` surface backgrounds with CSS token equivalents
2. Replace ALL `border: "1px solid rgba(255,255,255,0.xx)"` with `border: "1px solid var(--border-subtle)"`
3. Replace ALL Tailwind classes `bg-white/[0.xx]` → `bg-muted` or inline style with `var(--muted)`
4. Replace ALL `border-white/[0.xx]` Tailwind border classes → `border-border-subtle` Tailwind or inline `var(--border-subtle)`
5. Replace text colors: `text-gray-200` → `text-foreground`, `text-gray-400`/`text-gray-500` → `text-muted-foreground`
6. Keep emerald/cyan colors in brand identity elements (logo, active nav indicator, user badge) using SA_BRAND constant
7. Keep all Recharts stroke/fill colors unchanged

Files to process in order (shell first, content components after):
1. super-admin-shell.tsx — shell wrapper background
2. super-admin-sidebar.tsx — sidebar background, nav items, active indicator
3. super-admin-topbar.tsx — header border, badge backgrounds
4. dashboard-kpis.tsx, platform-stats-charts.tsx, recent-activity-list.tsx — dashboard widget cards
5. primarii-shared.tsx, primarii-grid.tsx, primarii-table.tsx, primarii-status-list.tsx — primarii list
6. admins-shared.tsx, admins-grid.tsx, admins-table.tsx — admins list
7. admin-detail-drawer.tsx, primarie-detail-drawer.tsx — drawers
8. sa-dashboard-content.tsx, sa-primarii-content.tsx, sa-admins-content.tsx, sa-audit-content.tsx — content wrappers
9. sa-analytics-content.tsx, sa-settings-content.tsx — analytics/settings (visual prototypes)
10. pagination.tsx, glass-tooltip.tsx — utility components
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert shell, sidebar, topbar to CSS tokens</name>
  <files>
    src/app/admin/primariata/_components/super-admin-shell.tsx,
    src/app/admin/primariata/_components/super-admin-sidebar.tsx,
    src/app/admin/primariata/_components/super-admin-topbar.tsx
  </files>
  <action>
Read each file, identify all inline `style={{ background/backgroundColor/borderColor }}` and Tailwind color classes, and replace with CSS token equivalents per the mapping in the interfaces block.

**super-admin-shell.tsx:**
- `style={{ background: "#09090f" }}` → `className="bg-background"` (remove the style prop, use Tailwind)
- Toaster inline style: `style={{ background: "#1a1a2e", border: "1px solid rgba(255,255,255,0.08)", color: "#fff" }}` → replace with CSS var equivalents: `style={{ background: "var(--popover)", border: "1px solid var(--border-subtle)", color: "var(--foreground)" }}`

**super-admin-sidebar.tsx:**
- Main aside: `style={{ background: "linear-gradient(180deg, #0c0c18 0%, #08080f 100%)" }}` → `style={{ background: "var(--sidebar-background)" }}`
- Logo gradient (`background: "linear-gradient(135deg, #10b981, #06b6d4)"`) → KEEP (brand identity)
- Quick search button: `background: "rgba(255,255,255,0.03)"` → `var(--muted)`, `border: "1px solid rgba(255,255,255,0.06)"` → `1px solid var(--border-subtle)`
- Nav active indicator: `background: "linear-gradient(135deg, rgba(16,185,129,0.15), rgba(6,182,212,0.08))"` / `border: "1px solid rgba(16,185,129,0.15)"` — this is brand identity, KEEP
- Section title color `text-gray-700` → `text-muted-foreground`
- Divider: `style={{ background: "rgba(255,255,255,0.06)" }}` → `style={{ background: "var(--border-subtle)" }}`
- Link text: `text-gray-500 hover:text-gray-200` → `text-muted-foreground hover:text-foreground`
- Nav link badge: `background: "linear-gradient(135deg, #10b981, #06b6d4)"` → KEEP (brand identity badge)
- Nav link dot badge: `style={{ background: "#10b981" }}` → KEEP
- Admin role badge container: `background: "rgba(255,255,255,0.03)"` → `var(--muted)`, `border: "1px solid rgba(255,255,255,0.04)"` → `1px solid var(--border-subtle)`
- User name: `text-gray-200` → `text-foreground`
- Zap icon color `text-emerald-400` → KEEP
- Toggle button: `bg-white/[0.03]` → `bg-muted`, `hover:bg-white/[0.06]` → `hover:bg-accent`, `text-gray-500` → `text-muted-foreground`

**super-admin-topbar.tsx:**
- Header `border-b border-white/[0.04]` → `border-b border-border` (Tailwind) or use `style={{ borderBottom: "1px solid var(--border-subtle)" }}` and remove the Tailwind class
- Super Admin badge: `background: "linear-gradient(135deg, rgba(16,185,129,0.08), rgba(6,182,212,0.05))"` / `border: "1px solid rgba(16,185,129,0.1)"` → KEEP (brand identity badge)
- Globe badge: `bg-white/[0.04]` → `bg-muted`
- Status badge: `bg-white/[0.04]` → `bg-muted`
- Text: `text-gray-300` → `text-muted-foreground`

After editing all three files, run:
```bash
cd /Users/thor/Documents/GitHub/primariata.work && pnpm type-check 2>&1 | grep "error TS" | grep -v "node_modules" | head -20
```
  </action>
  <verify>
    <automated>cd /Users/thor/Documents/GitHub/primariata.work && pnpm type-check 2>&1 | grep "error TS" | grep -v "node_modules" | head -20</automated>
  </verify>
  <done>Shell, sidebar, and topbar have no hardcoded surface/border hex colors. Brand emerald/cyan identity colors retained. CSS tokens used for backgrounds, borders, text. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Convert all _components content files to CSS tokens</name>
  <files>
    src/app/admin/primariata/_components/dashboard-kpis.tsx,
    src/app/admin/primariata/_components/platform-stats-charts.tsx,
    src/app/admin/primariata/_components/recent-activity-list.tsx,
    src/app/admin/primariata/_components/primarii-shared.tsx,
    src/app/admin/primariata/_components/primarii-grid.tsx,
    src/app/admin/primariata/_components/primarii-table.tsx,
    src/app/admin/primariata/_components/primarii-status-list.tsx,
    src/app/admin/primariata/_components/admins-shared.tsx,
    src/app/admin/primariata/_components/admins-grid.tsx,
    src/app/admin/primariata/_components/admins-table.tsx,
    src/app/admin/primariata/_components/admin-detail-drawer.tsx,
    src/app/admin/primariata/_components/primarie-detail-drawer.tsx,
    src/app/admin/primariata/_components/sa-dashboard-content.tsx,
    src/app/admin/primariata/_components/sa-primarii-content.tsx,
    src/app/admin/primariata/_components/sa-admins-content.tsx,
    src/app/admin/primariata/_components/sa-audit-content.tsx,
    src/app/admin/primariata/_components/sa-analytics-content.tsx,
    src/app/admin/primariata/_components/sa-settings-content.tsx,
    src/app/admin/primariata/_components/pagination.tsx,
    src/app/admin/primariata/_components/glass-tooltip.tsx,
    src/app/admin/primariata/_components/admins-shared.tsx,
    src/app/admin/primariata/_components/primarii-shared.tsx
  </files>
  <action>
Process each file systematically. For each file: read it, identify all `style={{ background/backgroundColor }}`, `style={{ border }}`, hardcoded hex colors in className or style props, and replace using the token mapping. Do NOT change Recharts `stroke`, `fill`, or `color` props that feed chart SVG elements.

**General substitution rules (apply to ALL files):**
- `background: "rgba(255,255,255,0.03)"` / `rgba(255,255,255,0.04)` / `rgba(255,255,255,0.025)` → `background: "var(--muted)"`
- `border: "1px solid rgba(255,255,255,0.04)"` / `rgba(255,255,255,0.05)"` / `rgba(255,255,255,0.06)"` → `border: "1px solid var(--border-subtle)"`
- `border: "1px solid rgba(255,255,255,0.08)"` → `border: "1px solid var(--border-strong)"`
- `background: rgba(14,14,28,0.95)` (drawer backdrop in admin-detail-drawer) → `background: "var(--popover)"`
- `borderLeft: "1px solid rgba(255,255,255,0.06)"` (drawer left border) → `borderLeft: "1px solid var(--border-subtle)"`
- Tailwind: `bg-white/[0.03]` / `bg-white/[0.04]` / `bg-white/[0.06]` → `bg-muted`
- Tailwind: `border-white/[0.04]` / `border-white/[0.05]` / `border-white/[0.06]` → remove and use inline `style={{ border: "1px solid var(--border-subtle)" }}`
- Tailwind: `text-gray-200` → `text-foreground`
- Tailwind: `text-gray-300` → `text-foreground`
- Tailwind: `text-gray-400` / `text-gray-500` / `text-gray-600` / `text-gray-700` → `text-muted-foreground`
- Tailwind: `text-white` → `text-foreground`
- `color: "#9ca3af"` (gray-400 inline) → `color: "var(--muted-foreground)"`
- `color: "#6b7280"` (gray-500 inline) → `color: "var(--muted-foreground)"`

**Status badge colors in primarii-shared.tsx and admins-shared.tsx (config objects):**
Status configs use specific semantic hex colors like `#10b981` (active/green), `#ef4444` (suspended/red), `#f59e0b` (pending/amber). These map to status tokens:
- Green/active: `var(--status-success)` background with opacity, or keep specific hex if it's a status indicator (these are semantic, not generic surface colors)
- Red/suspended: `var(--destructive)` or keep `#ef4444`
- Amber/pending: `var(--status-warning)` or keep `#f59e0b`
- Keep status indicator hex values as-is — they convey semantic meaning and do not need CSS var substitution

**Recharts exception (in platform-stats-charts.tsx, admin-detail-drawer.tsx, sa-primarii-content.tsx, sa-admins-content.tsx, sa-dashboard-content.tsx):**
Do NOT change any of these:
- `<Line stroke="#10b981" />`
- `<Bar fill="#06b6d4" />`
- `<Area stroke="#10b981" fill="rgba(16,185,129,0.1)" />`
- Recharts `Tooltip contentStyle={{ background: "...", border: "..." }}` props

**glass-tooltip.tsx:**
This is a custom tooltip with glassmorphism styling. Replace backdrop surface colors:
- Container background `rgba(...)` → `var(--popover)` or `var(--surface-overlay)`
- Border `rgba(255,255,255,0.xx)` → `var(--border-subtle)`
- Text colors → `text-foreground` / `text-muted-foreground`

**pagination.tsx:**
- Page button inactive: `background: "rgba(255,255,255,0.03)"` → `var(--muted)`
- Page button border → `var(--border-subtle)`
- Active page indicator gradient → KEEP (brand identity)
- Text colors: `text-gray-500` → `text-muted-foreground`, `text-gray-300` → `text-foreground`

Process files in batches. Read each file, make substitutions, write back. Prioritize correctness over speed — do one file at a time if needed.

After processing all files, run type-check:
```bash
cd /Users/thor/Documents/GitHub/primariata.work && pnpm type-check 2>&1 | grep "error TS" | grep -v "node_modules" | head -20
```
  </action>
  <verify>
    <automated>cd /Users/thor/Documents/GitHub/primariata.work && grep -rn '"#0[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f]"\|rgba(255,255,255,0\.' src/app/admin/primariata/_components/ 2>/dev/null | grep -v "CHART_COLORS\|stroke\|fill\|chart\|recharts\|Recharts\|SA_BRAND\|10b981\|06b6d4\|emerald\|cyan\|ef4444\|f59e0b\|status" | head -20</automated>
  </verify>
  <done>All super admin _components/ files use CSS tokens for surface/border colors. Text colors use Tailwind semantic classes. Recharts chart colors unchanged. Status indicator hex values kept. No TypeScript errors.</done>
</task>

</tasks>

<verification>
After both tasks:
1. grep for residual `rgba(255,255,255,0.0` patterns in _components/ returns only Recharts and status-indicator exceptions
2. grep for `#09090f`, `#0c0c18`, `#0f0f1a`, `#1a1a2e` returns no matches in _components/
3. In browser: switching to light theme makes the super admin module use light surface colors (not still appear dark)
4. pnpm type-check passes with no errors
</verification>

<success_criteria>
All hardcoded dark-mode hex/rgba surface, border, and text colors replaced with CSS tokens:
- Shell: bg-background
- Sidebar: var(--sidebar-background), var(--border-subtle) borders, text-muted-foreground text
- Topbar: var(--border-subtle) header border
- All cards/panels: var(--muted) background, var(--border-subtle) borders
- All drawers: var(--popover) background
- Pagination: var(--muted) background, var(--border-subtle) borders
- Recharts SVG colors: unchanged (CHART_COLORS / inline semantic hex)
- Brand emerald/cyan identity colors (logo, active nav): unchanged
- Status indicator colors (#10b981, #ef4444, #f59e0b): unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/22-super-admin-module/22-07-SUMMARY.md`
</output>
